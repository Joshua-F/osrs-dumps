// 8900
[clientscript,port_task_info_draw](component $component, dbrow $dbrow, int $int2, int $int3)
if (if_getwidth($component) ! $int2 | if_getheight($component) ! $int3) {
    ~port_task_info_draw($component, $dbrow);
}
