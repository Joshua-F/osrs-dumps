// 1976
[clientscript,script1976](component $component, enum $enum)
if (if_hassub(enum(component, component, $enum, toplevel_osrs_stretch:mainmodal)) = false) {
    ~osm_showchat;
    if_setontimer(null, $component);
}
