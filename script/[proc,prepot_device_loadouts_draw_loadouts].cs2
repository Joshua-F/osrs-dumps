// 7362
[proc,prepot_device_loadouts_draw_loadouts](int $int)
db_listall(prepot_device_loadout_ui);
def_dbrow $dbrow = db_findnext;
while ($dbrow ! null) {
    ~prepot_device_loadouts_draw_loadout($dbrow, $int);
    $dbrow = db_findnext;
}
