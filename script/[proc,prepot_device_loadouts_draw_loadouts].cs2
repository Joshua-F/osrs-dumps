// 7362
[proc,prepot_device_loadouts_draw_loadouts](int $int0)
db_listall(prepot_device_loadout_ui);
def_dbrow $dbrow1 = db_findnext;
while ($dbrow1 ! null) {
    ~prepot_device_loadouts_draw_loadout($dbrow1, $int0);
    $dbrow1 = db_findnext;
}
