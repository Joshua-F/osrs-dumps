// 9407
[proc,script9407](dbrow $dbrow)(string)
def_string $string = db_getfield($dbrow, action:action_desc, 0);
if (cc_find_param(league_tasks:task_hotfix, param_678, $dbrow) = true & string_length(cc_param(param_2580)) > 0) {
    return(cc_param(param_2580));
}
return($string);
