// 6430
[clientscript,script6430](component $component, int $int, obj $obj)
if (cc_find($component, $int) = true) {
    if (%magic_spellbook_tooltip = $obj) {
        cc_setoutline(2);
    } else {
        cc_setoutline(0);
    }
}
