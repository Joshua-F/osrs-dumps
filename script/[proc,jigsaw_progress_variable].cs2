// 7954
[proc,jigsaw_progress_variable](dbrow $dbrow0)(int)
switch_dbrow ($dbrow0) {
    case jigsaw_scrambled :
        return(%scrambled_puzzle);
    case default :
        return(%jigsaw_puzzle_solution);
}
