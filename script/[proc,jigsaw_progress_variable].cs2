// 7954
[proc,jigsaw_progress_variable](dbrow $dbrow)(int)
switch_dbrow ($dbrow) {
    case jigsaw_scrambled :
        return(%scrambled_puzzle);
    case default :
        return(%jigsaw_puzzle_solution);
}
