// 2095
[clientscript,script2095](component $component0, int $int1, int $int2)
if (cc_find($component0, $int1) = ^true) {
    if (clientclock >= $int2) {
        cc_delete;
    } else {
        ~script2096($component0);
    }
}
