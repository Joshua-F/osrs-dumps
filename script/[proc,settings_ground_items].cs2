// 7112
[proc,settings_ground_items]
if (~on_enhanced_any = false) {
    return;
}
%ground_items_enabled = calc(1 - %ground_items_enabled);
if (~toplevel_is_mobile(~toplevel_getcomponents) = true) {
    ~script7429(24);
}
