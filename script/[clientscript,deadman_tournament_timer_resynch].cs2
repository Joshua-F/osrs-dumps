// 1637
[clientscript,deadman_tournament_timer_resynch](component $component0)
if (~wildwars_world = true) {
    ~deadman_tournament_timer_resynch($component0, %varplayerbit_5710, clientclock);
} else {
    ~deadman_tournament_timer_resynch($component0, %varplayerbit_4164, clientclock);
}
