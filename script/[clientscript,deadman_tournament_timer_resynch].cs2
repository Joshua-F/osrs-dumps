// 1637
[clientscript,deadman_tournament_timer_resynch](component $component)
if (~wildwars_world = true) {
    ~deadman_tournament_timer_resynch($component, %wildwars_timer);
} else {
    ~deadman_tournament_timer_resynch($component, %deadman_tournament_timer);
}
