// 1637
[clientscript,deadman_tournament_timer_resynch](component $component0)
if (~wildwars_world = true) {
    ~deadman_tournament_timer_resynch($component0, %wildwars_timer);
} else {
    ~deadman_tournament_timer_resynch($component0, %deadman_tournament_timer);
}
