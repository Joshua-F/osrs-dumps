// 1637
[clientscript,deadman_tournament_timer_resynch](component $component)
if (~wildwars_world = true) {
    ~deadman_tournament_timer_resynch($component, %varplayerbit_5710, clientclock);
} else {
    ~deadman_tournament_timer_resynch($component, %varplayerbit_4164, clientclock);
}
