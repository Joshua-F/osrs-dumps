// 2786
[proc,script2786](stat $stat)(int)
switch_stat ($stat) {
    case ranged, magic :
        return(calc(pow(stat_base($stat), 2) / 600 * 32));
    case prayer :
        return(calc(pow(stat_base($stat), 2) / 600 * 18));
}
return(calc(pow(stat_base($stat), 2) / 600 * 35));
