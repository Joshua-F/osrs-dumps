// 8964
[proc,script8964](int $int0, dbrow $dbrow)(boolean)
if ($dbrow = null | $int0 <= 0) {
    return(false);
}
def_int $int2 = db_getfieldcount($dbrow, music_area_group:area);
def_int $int3 = 0;
while ($int3 < $int2) {
    if (db_getfield($dbrow, music_area_group:area, $int3) = $int0) {
        return(true);
    }
    $int3 = calc($int3 + 1);
}
return(false);
