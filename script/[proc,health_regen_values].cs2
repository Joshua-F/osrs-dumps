// 6050
[proc,health_regen_values](int $int)(int, int)
if (%last_health_regen_synch > 0) {
    $int = calc($int - %last_health_regen_synch);
    $int = calc($int * 30);
} else {
    $int = 0;
}
return(getbit_range(%last_regen_rate, 0, 10), $int);
