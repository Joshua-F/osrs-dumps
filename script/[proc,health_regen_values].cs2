// 6050
[proc,health_regen_values](int $int0)(int, int)
if (%last_health_regen_synch > 0) {
    $int0 = calc($int0 - %last_health_regen_synch);
    $int0 = calc($int0 * 30);
} else {
    $int0 = 0;
}
return(getbit_range(%last_regen_rate, 0, 10), $int0);
