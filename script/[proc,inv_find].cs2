// 9635
[proc,inv_find](inv $inv, obj $obj, int $int2)(int)
def_int $int3 = inv_size($inv);
while ($int2 < $int3) {
    if (inv_getobj($inv, $int2) = $obj) {
        return($int2);
    }
    $int2 = calc($int2 + 1);
}
return(-1);
