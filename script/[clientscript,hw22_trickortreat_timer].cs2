// 6920
[clientscript,hw22_trickortreat_timer](int $int0, component $component)
def_int $int2 = ~max(0, calc(50 * 30 - (clientclock - $int0)));
if ($int2 <= 0) {
    if_settext("0", $component);
} else {
    if_settext(tostring(calc($int2 / 50 + 1)), $component);
}
