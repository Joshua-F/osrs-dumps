// 9443
[proc,script9443](component $component0, int $int1, component $component2, string $string, int $int3, int $int4, int $int5)
if (~on_enhanced_mobile = true) {
    return;
}
if (~on_enhanced_any = false & $component2 ! null & if_find($component2) = true) {
    cc_setparam(param_2355, clientclock);
    if (cc_param(param_2352) ! null & (cc_param(param_2353) ! $int1 | cc_param(param_2352) ! $component0 | string_length(cc_param(param_2354)) ! string_length($string) | compare(cc_param(param_2354), $string) ! 0)) {
        ~deltooltip_action($component2);
    }
}
if (~on_enhanced_any = false & %tooltip_time < calc(clientclock + $int3)) {
    if (%tooltip_time < clientclock) {
        %tooltip_time = clientclock;
    }
    %tooltip_time = calc(%tooltip_time + 2);
    return;
}
%tooltip_time = calc(clientclock + $int3 + 10);
if (%tooltip_built = 0 & if_gethide($component2) = false) {
    ~script9444($component0, $int1, $component2, $string, $int4, $int5);
}
