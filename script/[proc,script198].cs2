// 198
[proc,script198](string $string0, string $string1)(int)
def_int $int0 = string_length($string1);
if ($int0 <= 0) {
    return(-1);
}
def_int $int1 = string_length($string0);
if ($int1 < $int0) {
    return(-1);
}
def_int $int2 = calc($int1 - $int0);
while ($int2 >= 0) {
    if (compare(substring($string0, $int2, calc($int2 + $int0)), $string1) = 0) {
        return($int2);
    }
    $int2 = calc($int2 - 1);
}
return(-1);
