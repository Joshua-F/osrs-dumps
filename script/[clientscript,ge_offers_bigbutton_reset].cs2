// 543
[clientscript,ge_offers_bigbutton_reset](component $component, string $string, int $int)
if (clientclock < $int) {
    return;
}
if_setontimer(null, $component);
~ge_offers_bigbutton($component, $string, 0);
