// 4404
[clientscript,clan_sidepanel_refresh_reset](int $int0, component $component, int $int2)
if (clientclock < $int0) {
    return;
}
if (cc_find($component, $int2) = true) {
    ~clan_sidepanel_refresh_reset($component);
}
