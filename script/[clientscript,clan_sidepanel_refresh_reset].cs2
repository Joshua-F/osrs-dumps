// 4404
[clientscript,clan_sidepanel_refresh_reset](int $int0, component $component1, int $int2)
if (clientclock < $int0) {
    return;
}
if (cc_find($component1, $int2) = ^true) {
    ~clan_sidepanel_refresh_reset($component1);
}
