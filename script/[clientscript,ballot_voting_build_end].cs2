// 8400
[clientscript,ballot_voting_build_end]
~ballot_add_button(-100, -101, "Submit", "Submit");
cc_setonop("script8401");
if (if_find(ballot:voting) = ^true) {
    if_setscrollsize(0, ~script8367(ballot:voting), ballot:voting);
    if_setscrollpos(0, 0, ballot:voting);
}
~scrollbar_default(ballot:voting_scrollbar, ballot:voting);
if_sethide(false, ballot:voting);
if_sethide(false, ballot:voting_scrollbar);
if_sethide(true, ballot:loading_container);
