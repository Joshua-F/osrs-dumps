// 889
[clientscript,ii_impling_count](component $component, obj $obj)
def_int $int = inv_total(inv, $obj);
if ($int > 0) {
    if_setcolour(^red, $component);
} else {
    if_setcolour(0xb5b54a, $component);
}
if_settext(tostring($int), $component);
