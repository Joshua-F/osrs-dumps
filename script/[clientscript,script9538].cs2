// 9538
[clientscript,script9538]
def_int $int = 0;
while ($int < 20) {
    ~script9542($int);
    ~debug_mes_info("", "%banktags_headers_slot<tostring($int)> = <tostring(~script9569($int))>");
    $int = calc($int + 1);
}
