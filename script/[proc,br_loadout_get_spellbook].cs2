// 6524
[proc,br_loadout_get_spellbook](int $int0)(int)
switch_int ($int0) {
    case 1 :
        return(%br_loadout_b_spellbook);
    case 2 :
        if (enum(int, int, enum_4389, %br_loadout_c_spellbook) = 2) {
            ~br_loadout_set_spellbook($int0, %br_loadout_c_spellbook);
        }
        return(%br_loadout_c_spellbook);
}
return(%br_loadout_a_spellbook);
