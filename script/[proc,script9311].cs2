// 9311
[proc,script9311](dbrow $dbrow)(boolean)
def_int $int = 1;
while ($int <= 100) {
    if (~script9318($int) = null) {
        ~script9319($int, $dbrow);
        return(true);
    }
    $int = calc($int + 1);
}
return(false);
