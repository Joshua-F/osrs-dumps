// 4574
[proc,settings_side_dropdown_update](component $component, enum $enum, int $int)
if (cc_find($component, 4) = true) {
    cc_settext(enum(int, string, $enum, ~settings_get_dropdown($int)));
}
