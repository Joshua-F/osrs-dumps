// 5298
[clientscript,options_bigbutton_reset](int $int, component $component, string $string, graphic $graphic)
if (clientclock >= $int) {
    ~options_bigbutton_init($component, $string, $graphic);
}
