// 7626
[proc,script7626](int $int0)(coord)
def_dbrow $dbrow1 = ~script3994($int0);
if ($dbrow1 ! null) {
    return(db_getfield($dbrow1, region_data:area_teleport_coord, 0));
} else {
    return(null);
}
