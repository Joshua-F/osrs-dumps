// 7626
[proc,script7626](int $int)(coord)
def_dbrow $dbrow = ~script3994($int);
if ($dbrow ! null) {
    return(db_getfield($dbrow, region_data:area_teleport_coord, 0));
} else {
    return(null);
}
