// 4733
[proc,script4733](component $component0, int $int1)
switch_component ($component0) {
    case hpbar_hud:hpdodger :
        %dodger_tracker = setbit(%dodger_tracker, 0);
}
if (testbit($int1, 31) = ^true) {
    %dodger_tracker = setbit(%dodger_tracker, 1);
}
