// 1640
[proc,deadman_tournament_timer_update](int $int0)
$int0 = calc($int0 - clientclock);
if ($int0 < 0) {
    $int0 = calc(600 * 30 + $int0);
}
$int0 = calc($int0 / 50);
def_int $int1 = calc($int0 / 60);
def_int $int2 = calc($int0 % 60);
if ($int2 < 10) {
    if_settext("<tostring($int1)>:0<tostring($int2)>", interface_90:com_10);
} else {
    if_settext("<tostring($int1)>:<tostring($int2)>", interface_90:com_10);
}
