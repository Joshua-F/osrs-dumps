// 6051
[proc,hitpoints_cape_values](int $int)(int, int)
if (%last_hitpoints_cape_regen_synch > 0) {
    $int = calc($int - %last_hitpoints_cape_regen_synch);
    $int = calc($int * 30);
} else {
    $int = 0;
}
return(getbit_range(%last_regen_rate, 22, 31), $int);
