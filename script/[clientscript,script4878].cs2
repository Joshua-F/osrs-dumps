// 4878
[clientscript,script4878](boolean $boolean, component $component, int $int2, int $int3, int $int4)
if (cc_find($component, $int2) = true) {
    if ($boolean = true) {
        cc_settrans($int3);
        cc_setontimer("script4879(calc(clientclock + 2), event_com, event_comsubid, $int4)");
    } else {
        cc_settrans($int4);
        cc_setontimer(null);
    }
}
