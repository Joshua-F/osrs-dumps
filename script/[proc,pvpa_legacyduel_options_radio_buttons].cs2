// 6175
[proc,pvpa_legacyduel_options_radio_buttons]
def_component $component = null;
def_int $int = 0;
while ($int <= 13) {
    $component = enum(int, component, enum_4209, $int);
    if ($component ! null & cc_find($component, 1) = true) {
        if (testbit(%varplayer_286, $int) = ^true) {
            cc_setgraphic("options_radio_buttons,2");
        } else {
            cc_setgraphic("options_radio_buttons,0");
        }
    }
    $int = calc($int + 1);
}
$int = inv_size(inv_94);
while ($int > 0) {
    $int = calc($int - 1);
    $component = enum(int, component, enum_4285, $int);
    if ($component ! null) {
        if (testbit(%varplayerbit_642, $int) = ^true) {
            if_sethide(false, $component);
            if_setonmouserepeat("tooltip_mouserepeat(event_com, -1, interface_755:com_99, enum(int, string, enum_4388, $int), 25, 300)", $component);
            if_setonmouseleave("deltooltip(interface_755:com_99)", $component);
        } else {
            if_sethide(true, $component);
            if_setonmouserepeat(null, $component);
            if_setonmouseleave(null, $component);
            ~deltooltip_action(interface_755:com_99);
        }
    }
}
