// 9452
[proc,script9452]()(boolean)
db_listall(quetzal);
def_dbrow $dbrow = db_findnext;
while ($dbrow ! null) {
    if (~script5015($dbrow) = false) {
        return(false);
    }
    $dbrow = db_findnext;
}
return(true);
