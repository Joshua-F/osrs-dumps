// 2859
[clientscript,clock_fake_update](component $component0)
if (calc(clientclock % 50) ! 0) {
    return;
}
if (calc(%varclient_375 + 3000) < clientclock) {
    %varplayerbit_8354 = calc(%varplayerbit_8354 + 1);
    ~clock_set($component0);
}
