// 2523
[clientscript,script2523](boolean $boolean, int $int1)
def_component $textlayer = questjournal:textlayer;
def_component $scrollbar = questjournal:scrollbar;
cc_deleteall($textlayer);
def_int $int4 = ^max_32bit_int;
if ($boolean = true) {
    $int4 = 0;
}
~quest_journal_scrollbar($scrollbar, $textlayer, $int1, $int4);
