// 2427
[clientscript,stonebutton_hoverreset](int $int0, string $string, component $component, graphic $graphic, int $int3)
if (clientclock >= $int0) {
    if_setontimer(null, $component);
    ~stonebutton_draw(false, $string, $graphic, $int3, $component);
}
