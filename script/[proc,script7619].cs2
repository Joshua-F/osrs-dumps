// 7619
[proc,script7619](int $int)(graphic)
def_dbrow $dbrow = ~script3994($int);
if ($dbrow ! null) {
    return(db_getfield($dbrow, region_data:map_shield_sprite, 0));
} else {
    return(null);
}
