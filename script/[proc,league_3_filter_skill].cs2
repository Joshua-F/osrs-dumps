// 5833
[proc,league_3_filter_skill](int $int)(boolean)
if (%league_task_filter_skill > 5 | %league_task_filter_skill <= 0) {
    return(true);
}
def_stat $stat = enum(int, stat, enum_681, $int);
if (%league_task_filter_skill = 5) {
    if (~script5830($stat) = 1) {
        return(true);
    } else {
        return(false);
    }
} else if (enum(stat, int, enum_4050, $stat) = %league_task_filter_skill) {
    return(true);
} else {
    return(false);
}
