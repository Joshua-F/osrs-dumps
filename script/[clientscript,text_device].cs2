// 2268
[clientscript,text_device](component $left_text, int $int, string $string0, string $string1)
if ($int = -1 & if_find($left_text) = true | cc_find($left_text, $int) = true) {
    if (~on_mobile = true) {
        cc_settext($string1);
    } else {
        cc_settext($string0);
    }
}
