// 7344
[proc,prepot_device_draw_inventory]
def_int $int = inv_size(inv_93);
while ($int > 0) {
    $int = calc($int - 1);
    if (cc_find(interface_886:com_12, $int) = true) {
        ~prepot_device_draw_inventory_slot(inv_getobj(inv_93, $int), inv_getnum(inv_93, $int));
    }
}
