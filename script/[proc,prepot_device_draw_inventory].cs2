// 7344
[proc,prepot_device_draw_inventory]
def_int $int = inv_size(inv);
while ($int > 0) {
    $int = calc($int - 1);
    if (cc_find(prepot_device:inventory_contents, $int) = true) {
        ~prepot_device_draw_inventory_slot(inv_getobj(inv, $int), inv_getnum(inv, $int));
    }
}
