// 144
[clientscript,interface_flash_fade](component $component, int $int1, int $int2, int $int3)
def_int $int4 = 0;
if (cc_find($component, $int1) = true) {
    if (clientclock >= $int3) {
        cc_delete;
        return;
    } else if (calc(clientclock % 40) < 20) {
        $int4 = calc(clientclock - $int2);
        $int4 = calc($int4 * 255);
        $int4 = calc($int4 / ($int3 - $int2));
        cc_settrans($int4);
    } else {
        cc_settrans(255);
    }
}
