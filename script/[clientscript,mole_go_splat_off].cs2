// 897
[clientscript,mole_go_splat_off](component $component, int $int1, int $int2)
if (cc_find($component, $int1) = true) {
    if (clientclock >= $int2) {
        cc_delete;
    } else {
        ~mole_splat_zoom($component);
    }
}
