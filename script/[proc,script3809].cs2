// 3809
[proc,script3809](int $int0, int $int1, stat $stat2, int $int3)(boolean)
if (enum(stat, int, enum_81, $stat2) = -1 | testbit($int1, enum(stat, int, enum_81, $stat2)) = ^false) {
    if ($int3 = 1) {
        mes("This skill is not available.");
    }
    return(false);
}
if (map_members = false & enum(stat, boolean, enum_1497, $stat2) = true) {
    if ($int3 = 1) {
        mes("You need to be on a members' world to gain experience in <enum(stat, string, enum_680, $stat2)>.");
    }
    return(false);
}
if (stat_base($stat2) < $int0) {
    if ($int3 = 1) {
        mes("You need level <tostring($int0)> <enum(stat, string, enum_680, $stat2)> to choose that option.");
    }
    return(false);
}
def_boolean $boolean4 = false;
def_string $string0 = "";
$boolean4, $string0 = ~script8948($stat2);
if ($boolean4 = true) {
    if ($int3 = 1) {
        mes($string0);
    }
    return(false);
}
switch_stat ($stat2) {
    case herblore :
        if (%druidquest < 4) {
            if ($int3 = 1) {
                mes("You need to complete the Druidic Ritual quest to choose that option.");
            }
            return(false);
        }
    case runecraft :
        if (%runemysteries < 6) {
            if ($int3 = 1) {
                mes("You need to complete the Rune Mysteries quest to choose that option.");
            }
            return(false);
        }
    case construction :
        if (%poh_house_location = 0) {
            if ($int3 = 1) {
                mes("You must own a house to choose that option.");
            }
            return(false);
        }
    case sailing :
        if (%sailing_intro < 50) {
            if ($int3 = 1) {
                mes("You need to complete the Pandemonium quest to choose that option.");
            }
            return(false);
        }
}
if (%league_type = 3 & ~league_world = true & ~script5830($stat2) = 0) {
    if ($int3 = 1) {
        mes("You haven't unlocked this skill yet.");
    }
    return(false);
}
return(true);
