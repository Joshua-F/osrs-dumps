// 9091
[proc,script9091](dbrow $dbrow0)(string)
if ($dbrow0 = null) {
    return("");
}
def_string $string0 = db_getfield($dbrow0, sailing_dock:inline_name, 0);
if (string_length($string0) <= 0) {
    $string0 = db_getfield($dbrow0, sailing_dock:nice_name, 0);
}
return($string0);
