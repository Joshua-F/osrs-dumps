// 4298
[clientscript,script4298](int $int, string $string, component $component, graphic $graphic)
if (clientclock >= $int) {
    if_setontimer(null, $component);
    ~clan_stonebutton_draw(false, $string, $graphic, $component);
}
