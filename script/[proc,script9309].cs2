// 9309
[proc,script9309](dbrow $dbrow)(boolean)
def_int $int = 1;
while ($int <= 100) {
    if (~script9316($int) = $dbrow) {
        ~script9317($int, null);
        return(true);
    }
    $int = calc($int + 1);
}
return(false);
