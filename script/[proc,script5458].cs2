// 5458
[proc,script5458](stat $stat, int $int1)
def_int $int2 = calc(10 - 1);
while ($int2 > 0) {
    ~xptracker_set_stat_xp($stat, $int2, ~xptracker_get_stat_xp($stat, calc($int2 - 1)));
    $int2 = calc($int2 - 1);
}
~xptracker_set_stat_xp($stat, 0, $int1);
