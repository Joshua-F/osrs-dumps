// 9099
[proc,script9099](int $int0)(dbrow)
def_int $int1 = db_find(sailing_sea:unique_id, $int0);
if ($int1 = 0) {
    ~script2075("Sailing: No sea found with ID <tostring($int0)>.");
} else if ($int1 > 1) {
    ~script2075("Sailing: <tostring($int1)> seas found with ID <tostring($int0)>.");
}
return(db_findnext);
