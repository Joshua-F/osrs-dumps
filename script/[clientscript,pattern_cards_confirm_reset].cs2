// 347
[clientscript,pattern_cards_confirm_reset](int $int, component $component)
if (clientclock < $int) {
    return;
}
if_setontimer(null, $component);
~stonebutton_init("Confirm", $component, p12_full, 0);
if_setonop("pattern_cards_confirm_op(event_op, event_com)", $component);
