// 5102
[proc,reportabuse_toggle_get](boolean $boolean)(boolean)
if ($boolean = true) {
    return(%snapshot_ignore);
}
if (playermod = 1 | staffmodlevel > 0) {
    return(%snapshot_mute);
}
return(false);
