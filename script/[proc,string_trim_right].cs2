// 5532
[proc,string_trim_right](string $string)(string)
def_int $int = string_length($string);
while ($int > 0) {
    if (compare(substring($string, calc($int - 1), $int), " ") ! 0) {
        return(substring($string, 0, $int));
    }
    $int = calc($int - 1);
}
return("");
