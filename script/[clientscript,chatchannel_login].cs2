// 3765
[clientscript,chatchannel_login](string $string0)
if (string_length($string0) > 0) {
    if (string_length(friendschat_getchatownername) > 0) {
        friendschat_leavechat;
    }
    ~meslayer_initlastnames;
    %last_friendschatjoin = removetags($string0);
    friendschat_joinchat($string0);
}
