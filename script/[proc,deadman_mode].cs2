// 2248
[proc,deadman_mode]()(int)
def_int $int = worldflags;
if (testbit($int, 25) = ^true) {
    return(3);
}
if (testbit($int, 29) = ^true) {
    if (testbit($int, 26) = ^true) {
        return(3);
    }
    if (testbit($int, 30) = ^true) {
        return(2);
    }
    return(1);
}
return(0);
