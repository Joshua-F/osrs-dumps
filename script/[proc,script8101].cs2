// 8101
[proc,script8101](int $int0)(obj)
def_dbrow $dbrow1 = ~script8100($int0);
def_obj $obj2 = null;
if ($dbrow1 ! null) {
    $obj2 = db_getfield($dbrow1, sailing_combat_facility_ammunition:ammunition_obj, 0);
} else {
    ~error("$ammunition_data was found to be null for $id = <tostring($int0)>.");
}
return($obj2);
