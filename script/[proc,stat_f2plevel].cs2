// 1320
[proc,stat_f2plevel]()(int)
def_int $int0 = 0;
def_int $int1 = 1;
def_stat $stat = enum(int, stat, enum_681, $int1);
while ($stat ! null) {
    if (enum(stat, boolean, enum_1497, $stat) = false) {
        $int0 = calc($int0 + stat_base($stat));
    }
    $int1 = calc($int1 + 1);
    $stat = enum(int, stat, enum_681, $int1);
}
return($int0);
