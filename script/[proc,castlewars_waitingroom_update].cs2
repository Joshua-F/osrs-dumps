// 2375
[proc,castlewars_waitingroom_update](component $component)
if (%castlewars_timer <= 0) {
    if_settext("Waiting for players to join the other team.", $component);
} else {
    if_settext("Time until next game starts: <tostring(%castlewars_timer)>", $component);
}
