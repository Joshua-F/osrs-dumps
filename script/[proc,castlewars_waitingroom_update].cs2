// 2375
[proc,castlewars_waitingroom_update](component $component0)
if (%castlewars_timer <= 0) {
    if_settext("Waiting for players to join the other team.", $component0);
} else {
    if_settext("Time until next game starts: <tostring(%castlewars_timer)>", $component0);
}
