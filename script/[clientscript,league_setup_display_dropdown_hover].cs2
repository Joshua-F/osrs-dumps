// 3703
[clientscript,league_setup_display_dropdown_hover](boolean $boolean, component $component, int $int2, int $int3, int $rgb)
if (cc_find($component, $int2) = true & .cc_find($component, $int3) = true) {
    cc_setcolour($rgb);
    if ($boolean = true) {
        .cc_sethide(false);
        .cc_setposition(cc_getx, cc_gety, ^setpos_abs_left, ^setpos_abs_top);
    } else {
        .cc_sethide(true);
    }
}
