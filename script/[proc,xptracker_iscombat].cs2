// 5381
[proc,xptracker_iscombat](stat $stat)(boolean)
if ($stat = attack | $stat = strength | $stat = ranged | $stat = defence | $stat = hitpoints | $stat = slayer) {
    return(true);
} else {
    return(false);
}
