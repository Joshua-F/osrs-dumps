// 6609
[clientscript,toa_invocations_init](component $scrollbar, component $invocations)
def_int $int2 = 0;
def_struct $struct = null;
def_int $int4 = enum_getoutputcount(enum_4664);
def_int $int5 = 0;
def_boolean $boolean = ~league_world;
cc_deleteall($invocations);
while ($int2 < $int4) {
    $struct = enum(int, struct, enum_4664, $int2);
    if (struct_param($struct, param_1161) = 15) {
        if ($boolean = true) {
            $int5 = ~script6610($int2, $struct, $invocations);
        }
    } else {
        $int5 = ~script6610($int2, $struct, $invocations);
    }
    $int2 = calc($int2 + 1);
}
if_setscrollsize(0, $int5, $invocations);
if_setscrollpos(0, 0, $invocations);
~scrollbar_vertical($scrollbar, $invocations, "scrollbar_dragger_v2,3", "scrollbar_dragger_v2,0", "scrollbar_dragger_v2,1", "scrollbar_dragger_v2,2", "scrollbar_v2,0", "scrollbar_v2,1");
