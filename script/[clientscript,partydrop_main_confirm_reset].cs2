// 3616
[clientscript,partydrop_main_confirm_reset](int $int, component $component)
if (clientclock < $int) {
    return;
}
if_setontimer(null, $component);
~clan_stonebutton_init("Confirm", $component, p12_full);
if_setonop("partydrop_main_confirm_op(event_op, event_com)", $component);
