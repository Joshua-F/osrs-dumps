// 131
[clientscript,closebutton_key](int $key)
if (~keypress_permit(null, null) = false) {
    return;
}
def_enum $enum = ~toplevel_getcomponents;
if ($enum = null) {
    return;
}
def_component $component = enum(component, component, $enum, interface_161:com_13);
if ($component ! null & $key = ^key_escape & %varplayerbit_4681 = 1 & if_hassub($component) = true) {
    if_close;
    return;
}
