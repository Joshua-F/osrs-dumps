// 5089
[proc,script5089](boolean $boolean0)
def_int $int1 = 0;
if ($boolean0 = true) {
    $int1 = string_length(%snapshot_name);
    if ($int1 > 0) {
        while ($int1 > 0 & compare(substring(%snapshot_name, calc($int1 - 1), $int1), " ") = 0) {
            %snapshot_name = substring(%snapshot_name, 0, calc($int1 - 1));
            $int1 = string_length(%snapshot_name);
        }
        if ($int1 > 0) {
            sound_synth(synth_2266, 1, 0);
            ~reportabuse_page2_set;
            return;
        }
    }
    sound_synth(synth_2277, 1, 0);
    ~reportabuse_hint_set(1);
} else {
    sound_synth(synth_2266, 1, 0);
}
~reportabuse_page1_set;
