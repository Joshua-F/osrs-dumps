// 919
[proc,toplevel_side_customise](enum $enum)
def_component $component1 = null;
def_component $component2 = null;
def_int $int3 = 0;
def_int $int4 = 0;
def_int $setposh = ^setpos_abs_left;
def_int $setposv = ^setpos_abs_top;
if ($enum = enum_1131 | $enum = enum_1745) {
    if (%toplevel_panel ! -1) {
        $component1 = enum(int, component, enum_1137, %toplevel_panel);
    }
    $component2 = enum(component, component, $enum, toplevel_pre_eoc:side_container);
    if ((if_hassub(enum(component, component, $enum, toplevel_osrs_stretch:sidemodal)) = true | $component1 ! null & if_hassub(enum(component, component, $enum, $component1)) = true) & ~script4138 = false) {
        if_sethide(false, $component2);
    } else {
        if_sethide(true, $component2);
    }
    if ($enum = enum_1745) {
        if (if_getheight(toplevel_osm:gameframe) < 503) {
            if (%osm_tab_expansion_disabled = 1) {
                $int3, $int4, $setposh, $setposv = 58, 4, ^setpos_abs_right, ^setpos_abs_bottom;
            } else {
                $int3, $int4, $setposh, $setposv = 85, 4, ^setpos_abs_right, ^setpos_abs_bottom;
            }
        } else if (%osm_tab_expansion_disabled = 1) {
            $int3, $int4, $setposh, $setposv = 58, 87, ^setpos_abs_right, ^setpos_abs_centre;
        } else {
            $int3, $int4, $setposh, $setposv = 85, 87, ^setpos_abs_right, ^setpos_abs_centre;
        }
        if (~script7830 = 1) {
            $setposh = ^setpos_abs_left;
            $int3 = 58;
        }
        if_setposition($int3, $int4, $setposh, $setposv, $component2);
        if_setposition(0, $int4, ^setpos_abs_right, $setposv, toplevel_osm:side_right_tabs);
    }
    ~script2526;
    %buff_bar_dodger_update = clientclock;
    ~script2386;
    if (if_gethide(enum(component, component, $enum, toplevel_osrs_stretch:side_top)) = true) {
        if_sethide(true, enum(component, component, $enum, toplevel_osrs_stretch:stone10));
        if_sethide(true, enum(component, component, $enum, toplevel_osrs_stretch:icon10));
    } else {
        if_sethide(false, enum(component, component, $enum, toplevel_osrs_stretch:stone10));
        if_sethide(false, enum(component, component, $enum, toplevel_osrs_stretch:icon10));
    }
}
