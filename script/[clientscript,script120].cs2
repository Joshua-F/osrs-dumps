// 120
[clientscript,script120]
def_component $component = ~script121(calc(%varplayerbit_3756 - 1));
if ($component ! null) {
    if (calc(clientclock % 20) > 9) {
        if_sethide(true, $component);
    } else {
        if_sethide(false, $component);
    }
}
