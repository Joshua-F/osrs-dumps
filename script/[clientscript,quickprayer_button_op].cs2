// 469
[clientscript,quickprayer_button_op](int $int0, obj $obj, component $component, int $int3, int $int4)
def_int $int5 = oc_param($obj, param_630);
if ($int0 = 1 & .cc_find($component, $int4) = true) {
    if (testbit(~quickprayer_selected_get, $int5) = ^true) {
        ~quickprayer_selected_set(clearbit(~quickprayer_selected_get, $int5));
    } else {
        if (~prayer_isavailable($obj) = false) {
            return;
        }
        ~quickprayer_selected_set(setbit(~quickprayer_selected_get, $int5));
    }
    sound_synth(synth_2266, 1, 0);
    if (cc_find($component, $int3) = true) {
        ~quickprayer_button_update($obj);
    }
}
