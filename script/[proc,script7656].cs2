// 7656
[proc,script7656](struct $struct0)(int)
def_int $int1 = 1;
def_int $int2 = -1;
def_int $int3 = 0;
def_int $int4 = 1;
if ($struct0 = null) {
    return(0);
}
if (struct_param($struct0, param_2029) > 0) {
    $int4 = struct_param($struct0, param_2029);
}
while ($int1 <= enum_getoutputcount(enum_5695) & $int3 = 0) {
    if (enum(int, struct, enum_5695, $int1) = $struct0) {
        $int3, $int2 = 1, $int1;
    } else {
        $int1 = calc($int1 + 1);
    }
}
switch_int ($int2) {
    case 1 :
        if (%league_combat_mastery_point_unlock_1 >= $int4) {
            return(1);
        }
    case 2 :
        if (%league_combat_mastery_point_unlock_2 >= $int4) {
            return(1);
        }
    case 3 :
        if (%league_combat_mastery_point_unlock_3 >= $int4) {
            return(1);
        }
    case 4 :
        if (%league_combat_mastery_point_unlock_4 >= $int4) {
            return(1);
        }
    case 5 :
        if (%league_combat_mastery_point_unlock_5 >= $int4) {
            return(1);
        }
    case 6 :
        if (%league_combat_mastery_point_unlock_6 >= $int4) {
            return(1);
        }
    case 7 :
        if (%league_combat_mastery_point_unlock_7 >= $int4) {
            return(1);
        }
    case 8 :
        if (%league_combat_mastery_point_unlock_8 >= $int4) {
            return(1);
        }
    case 9 :
        if (%league_combat_mastery_point_unlock_9 >= $int4) {
            return(1);
        }
    case 10 :
        if (%league_combat_mastery_point_unlock_10 >= $int4) {
            return(1);
        }
    case default :
        return(0);
}
return(0);
