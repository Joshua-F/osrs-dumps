// 2519
[proc,script2519]()(int)
def_enum $enum0 = ~toplevel_getcomponents;
if ($enum0 ! enum_1745) {
    return(0);
}
def_component $component1 = enum(int, component, enum_1137, %toplevel_panel);
def_component $component2 = enum(component, component, $enum0, toplevel_pre_eoc:side_container);
if (if_hassub(enum(component, component, $enum0, toplevel_osrs_stretch:sidemodal)) = true & if_hassub(enum(component, component, $enum0, toplevel_osrs_stretch:mainmodal)) = true) {
    if (%osm_tab_expansion_disabled = 1) {
        return(3);
    } else {
        return(4);
    }
}
if ($component1 ! null) {
    if (%osm_tab_expansion_disabled = 1) {
        return(3);
    } else {
        return(4);
    }
}
if (if_hassub(enum(component, component, $enum0, toplevel_osrs_stretch:sidemodal)) = true) {
    if (%osm_tab_expansion_disabled = 1) {
        return(3);
    } else {
        return(4);
    }
}
if (%osm_tab_expansion_disabled ! 1) {
    return(2);
}
return(1);
