// 7702
[clientscript,league_side_panel_misc_hover](boolean $boolean, component $component, int $rgb)
if ($boolean = true) {
    if_setcolour(^white, $component);
} else {
    if_setcolour($rgb, $component);
}
