// 433
[clientscript,grouping_dropdown](boolean $boolean)
sound_synth(synth_2266, 1, 0);
if ($boolean = true) {
    if_sethide(false, grouping:dropdown);
} else {
    if_sethide(true, grouping:dropdown);
}
~grouping_rebuild;
