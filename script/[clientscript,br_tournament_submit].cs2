// 1420
[clientscript,br_tournament_submit](int $int0, string $string, component $component, int $int2)
if (clientclock < $int0) {
    return;
}
resume_namedialog($string);
if (cc_find($component, $int2) = true) {
    cc_setcolour(^white);
    cc_setontimer(null);
}
