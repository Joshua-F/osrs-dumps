// 2967
[proc,adventurepath_return_path_length](int $int)(int)
if ($int = -1) {
    return(0);
}
def_enum $enum = enum(int, enum, enum_2620, $int);
if ($enum ! null) {
    return(enum_getoutputcount($enum));
}
return(0);
