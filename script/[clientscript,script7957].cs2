// 7957
[clientscript,script7957](dbrow $dbrow0)
def_int $int1 = db_getfieldcount($dbrow0, jigsaw:piece);
def_int $int2 = 0;
def_int $int3 = random($int1);
while ($int2 < $int1) {
    if ($int2 ! $int3) {
        ~script7955($dbrow0, $int2);
    }
    $int2 = calc($int2 + 1);
}
