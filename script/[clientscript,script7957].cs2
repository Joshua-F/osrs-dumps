// 7957
[clientscript,script7957](dbrow $dbrow)
def_int $int1 = db_getfieldcount($dbrow, jigsaw:piece);
def_int $int2 = 0;
def_int $int3 = random($int1);
while ($int2 < $int1) {
    if ($int2 ! $int3) {
        ~script7955($dbrow, $int2);
    }
    $int2 = calc($int2 + 1);
}
