// 452
[clientscript,clanchat_login](string $string)
if (string_length($string) > 0) {
    if (string_length(clan_getchatownername) > 0) {
        clan_leavechat;
    }
    ~meslayer_initlastnames;
    %varclient_362 = removetags($string);
    clan_joinchat($string);
}
