// 9238
[proc,script9238](component $component, int $int1, int $int2, int $int3)
def_int $int4 = %option_music;
def_int $int5 = ~minmax(0, 100, scale($int1, calc($int2 - 16), 100));
if (%option_music ! $int5 | $int3 = 1) {
    %option_music = ~minmax(0, 100, $int5);
    ~script2475;
    ~script9240($component, $int2);
    ~script9254(30);
    ~script9256;
    if ($int4 <= 0 & %option_music > 0 | $int4 > 0 & %option_music <= 0 | $int3 = 1) {
        if_runscript*(ifscript_406545006, settings_side:music_slider_bobble, -1)(%option_music);
    }
}
