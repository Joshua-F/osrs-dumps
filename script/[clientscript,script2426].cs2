// 2426
[clientscript,script2426](boolean $boolean, string $string, component $component, graphic $graphic, int $int)
~stonebutton_draw($boolean, $string, $graphic, $int, $component);
if ($boolean = true) {
    if_setontimer("stonebutton_hoverreset(calc(clientclock + 2), $string, event_com, $graphic, $int)", $component);
} else {
    if_setontimer(null, $component);
}
