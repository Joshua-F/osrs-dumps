// 4865
[proc,speedrunning_get_dbrow](dbrow $dbrow)(dbrow)
if (db_getrowtable($dbrow) = quest) {
    if (db_getfieldcount($dbrow, quest:speedrun) > 0) {
        return(db_getfield($dbrow, quest:speedrun, 0));
    }
    return(null);
}
return(null);
