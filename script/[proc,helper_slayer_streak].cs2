// 5322
[proc,helper_slayer_streak]()(string, string)
def_int $int = 0;
if (%slayer_master = 7) {
    $int = %slayer_wilderness_tasks_completed;
} else {
    $int = %slayer_tasks_completed;
}
if ($int >= 16000) {
    return("Streak: ", "Lots!");
}
return("Streak: ", tostring($int));
