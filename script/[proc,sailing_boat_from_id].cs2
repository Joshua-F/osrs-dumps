// 9010
[proc,sailing_boat_from_id](int $int0)(dbrow)
def_int $int1 = db_find(sailing_boat:type_id, $int0);
if ($int1 ! 1) {
    ~script2075("Invalid configuration for boat ID <tostring($int0)>, found <tostring($int1)>.");
    return(null);
}
return(db_findnext);
