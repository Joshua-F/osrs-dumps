// 4792
[proc,worldmap_tooltip_override](string $string0, mapelement $mapelement0)(string)
if (mec_category($mapelement0) = category_1056) {
    $string0 = ~script4814($mapelement0);
}
if (mec_category($mapelement0) = category_1104) {
    $string0 = ~script7317($mapelement0);
}
return($string0);
