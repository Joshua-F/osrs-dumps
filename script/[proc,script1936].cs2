// 1936
[proc,script1936](stat $stat0)(int)
switch_stat ($stat0) {
    case ranged :
        return(%deadman_xp_ranged);
    case magic :
        return(%deadman_xp_magic);
    case defence :
        return(%deadman_xp_defence);
    case strength :
        return(%deadman_xp_strength);
    case attack :
        return(%deadman_xp_attack);
    case hitpoints :
        return(%deadman_xp_hitpoints);
    case fletching :
        return(%deadman_xp_fletching);
    case mining :
        return(%deadman_xp_mining);
    case woodcutting :
        return(%deadman_xp_woodcutting);
    case herblore :
        return(%deadman_xp_herblore);
    case fishing :
        return(%deadman_xp_fishing);
    case prayer :
        return(%deadman_xp_prayer);
    case agility :
        return(%deadman_xp_agility);
    case thieving :
        return(%deadman_xp_thieving);
    case cooking :
        return(%deadman_xp_cooking);
    case hunter :
        return(%deadman_xp_hunter);
    case crafting :
        return(%deadman_xp_crafting);
    case runecraft :
        return(%deadman_xp_runecraft);
    case slayer :
        return(%deadman_xp_slayer);
    case farming :
        return(%deadman_xp_farming);
    case construction :
        return(%deadman_xp_construction);
    case firemaking :
        return(%deadman_xp_firemaking);
    case smithing :
        return(%deadman_xp_smithing);
}
return(-1);
