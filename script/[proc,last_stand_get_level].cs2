// 7769
[proc,last_stand_get_level](stat $stat)(int)
def_int $int = stat($stat);
if ($stat ! attack & $stat ! strength & $stat ! defence & $stat ! ranged & $stat ! magic) {
    return($int);
}
return(max(%last_stand_fakelevel, calc(stat_base($stat) + 15)));
