// 4325
[proc,clan_piano_play_note](int $int0)
def_enum $enum = enum_3799;
def_struct $struct = null;
def_int $int3 = enum_getoutputcount($enum);
if ($int0 < $int3) {
    $struct = enum(int, struct, $enum, $int0);
    sound_synth(struct_param($struct, param_1239), 1, 0);
    ~clan_piano_highlight_key($int0);
}
