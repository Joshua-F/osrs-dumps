// 4889
[clientscript,bankside_runepouch_loadout_button_hoverreset](int $int, component $component)
if (clientclock >= $int) {
    if_setontimer(null, $component);
    ~bankside_runepouch_loadout_button_draw(false, $component);
}
