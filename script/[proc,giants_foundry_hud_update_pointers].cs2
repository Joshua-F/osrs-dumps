// 6115
[proc,giants_foundry_hud_update_pointers]
def_component $quality_pointer = giants_foundry_hud:quality_pointer;
def_component $temperature_pointer = giants_foundry_hud:temperature_pointer;
def_component $completion_pointer = giants_foundry_hud:completion_pointer;
def_component $completion_bar_cover = giants_foundry_hud:completion_bar_cover;
def_component $completion_bar_highlight = giants_foundry_hud:completion_bar_highlight;
def_component $quality_bar = giants_foundry_hud:quality_bar;
def_component $quality_bar_fill = giants_foundry_hud:quality_bar_fill;
def_component $quality_bar_lost = giants_foundry_hud:quality_bar_lost;
def_component $hammer_bar_highlight = giants_foundry_hud:hammer_bar_highlight;
def_component $grind_bar_highlight = giants_foundry_hud:grind_bar_highlight;
def_component $polish_bar_highlight = giants_foundry_hud:polish_bar_highlight;
def_int $int11 = calc(if_getwidth($quality_bar) - 2);
def_int $int12 = 0;
$int12 = scale(%giants_foundry_preform_quality, 205, $int11);
if_setposition($int12, -1, ^setpos_abs_left, ^setpos_abs_bottom, $quality_pointer);
if_setsize(calc($int12 + 1), 2, ^setsize_abs, ^setsize_minus, $quality_bar_fill);
$int12 = scale(%giants_foundry_preform_start_quality, 205, $int11);
if_setsize(calc($int12 + 1), 2, ^setsize_abs, ^setsize_minus, $quality_bar_lost);
if (%giants_foundry_preform_quality = 0) {
    if_sethide(true, $quality_bar_fill);
} else {
    if_sethide(false, $quality_bar_fill);
}
$int12 = scale(%giants_foundry_preform_temperature, 1000, $int11);
if_setposition($int12, -1, ^setpos_abs_left, ^setpos_abs_bottom, $temperature_pointer);
$int12 = scale(%giants_foundry_preform_completion, 1000, $int11);
if_setposition($int12, -1, ^setpos_abs_left, ^setpos_abs_bottom, $completion_pointer);
def_int $int13 = ~giants_foundry_section_count;
def_int $int14 = ~script6120;
if (%giants_foundry_preform_completion >= 1000) {
    if_sethide(true, $completion_bar_highlight);
    if_sethide(false, $completion_bar_cover);
} else {
    if_sethide(false, $completion_bar_highlight);
    if_sethide(true, $completion_bar_cover);
}
def_int $int15 = calc($int11 / $int13);
def_int $int16 = calc($int11 % $int15);
$int12 = calc($int15 * $int14 + 7 + ~min($int16, $int14));
if ($int14 < $int16) {
    $int15 = calc($int15 + 1);
}
if_setposition($int12, 1, ^setpos_abs_left, ^setpos_abs_top, $completion_bar_highlight);
if_setsize($int15, 2, ^setsize_abs, ^setsize_minus, $completion_bar_highlight);
def_int $int17 = 0;
def_int $int18 = 0;
$int17, $int18 = ~script6121(0);
if (%giants_foundry_preform_temperature >= $int17 & %giants_foundry_preform_temperature <= calc($int17 + $int18)) {
    if_sethide(false, $hammer_bar_highlight);
} else {
    if_sethide(true, $hammer_bar_highlight);
}
$int17, $int18 = ~script6121(1);
if (%giants_foundry_preform_temperature >= $int17 & %giants_foundry_preform_temperature <= calc($int17 + $int18)) {
    if_sethide(false, $grind_bar_highlight);
} else {
    if_sethide(true, $grind_bar_highlight);
}
$int17, $int18 = ~script6121(2);
if (%giants_foundry_preform_temperature >= $int17 & %giants_foundry_preform_temperature <= calc($int17 + $int18)) {
    if_sethide(false, $polish_bar_highlight);
} else {
    if_sethide(true, $polish_bar_highlight);
}
