// 8184
[clientscript,script8184](int $key, component $component, int $int2, int $int3)
def_boolean $boolean = ~closebutton_key($key);
if ($boolean = false & $key = ^key_escape & %keybinding_esc_to_close = 1) {
    if_triggerop($component, $int2, $int3);
}
