// 473
[clientscript,quickprayer_close_timeout](component $component, int $int)
if (clientclock < $int) {
    return;
}
if_setgraphic("combatboxesmed,0", $component);
if_setontimer(null, $component);
