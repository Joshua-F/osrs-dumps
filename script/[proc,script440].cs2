// 440
[proc,script440](dbrow $dbrow)(string)
def_obj $obj = db_getfield($dbrow, poh_room:room_obj, 0);
def_string $string = "";
if ($obj ! null) {
    $string = oc_name($obj);
} else {
    $string = db_getfield($dbrow, poh_room:name, 0);
}
return($string);
