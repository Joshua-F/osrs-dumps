// 8978
[proc,sailing_boat_selection_get_title]()(string)
db_find(boat_selection_type:type, %sailing_boat_selection_type);
def_dbrow $dbrow = db_findnext;
if ($dbrow = null) {
    return("Boats");
}
return(db_getfield($dbrow, boat_selection_type:title, 0));
