// 5803
[proc,script5803](boolean $boolean, int $int1)(int)
def_int $int2 = -1;
def_int $int3 = 1;
def_int $int4 = 0;
def_int $int5 = 0;
def_struct $struct = null;
if (%league_type = 3) {
    $int2 = enum_getoutputcount(enum_4127);
    if ($boolean = true) {
        return(enum(int, int, enum_4127, $int2));
    }
    while ($int3 <= $int2) {
        $int5 = $int4;
        $int4 = enum(int, int, enum_4127, $int3);
        if ($int4 > %league_points_claimed) {
            if ($int1 = 1) {
                return($int5);
            } else {
                return($int4);
            }
        }
        $int3 = calc($int3 + 1);
    }
    return(-1);
} else {
    $struct = ~script3180($boolean, $int1);
    if ($struct = null) {
        return(-1);
    } else {
        return(struct_param($struct, param_877));
    }
}
