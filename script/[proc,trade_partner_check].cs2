// 210
[proc,trade_partner_check](int $chattype, string $string0, int $int)(boolean)
if (%varclient_41 ! 6) {
    return(false);
}
if (string_length(%varclient_357) <= 0) {
    return(false);
}
def_string $string1 = "";
if ($chattype = ^chattype_publicchat | $chattype = ^chattype_modchat | $chattype = ^chattype_privatechat | $chattype = ^chattype_privatechatout | $chattype = ^chattype_modprivatechat | $chattype = ^chattype_friendschat) {
    $string1 = removetags($string0);
    if (compare($string1, removetags(chat_playername)) = 0) {
        return(true);
    }
    if (compare($string1, removetags(%varclient_357)) = 0) {
        if ($chattype = ^chattype_modchat | $chattype = ^chattype_modprivatechat) {
            return(true);
        }
        if (chat_getfilter_trade = ^chatfilter_on) {
            return(true);
        }
        if (chat_getfilter_trade = ^chatfilter_friends & $int = 1) {
            return(true);
        }
    }
}
return(false);
