// 7600
[proc,combat_interface_glow_toggle](boolean $boolean)
def_component $glow = combat_interface:glow;
if ($boolean = true) {
    if_sethide(false, $glow);
    ~combat_interface_glow_setheight;
} else {
    if_sethide(true, $glow);
}
