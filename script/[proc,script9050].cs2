// 9050
[proc,script9050](int $int0)(dbrow)
db_find(sailing_crew:unique_id, $int0);
def_dbrow $dbrow1 = db_findnext;
if ($dbrow1 = null) {
    ~error("Could not find crew data for id <tostring($int0)>.");
    return(null);
}
return($dbrow1);
