// 9009
[proc,script9009](int $int0)(int)
def_int $int1 = 1;
def_int $int2 = -1;
while ($int1 <= 5) {
    if (~script9006($int1) = $int0) {
        if ($int2 = -1) {
            $int2 = $int1;
        } else {
            ~script2075("Player has two or more boat slot ids set to the same value! This is very bad! The value was: <tostring($int0)>");
            return($int2);
        }
    }
    $int1 = calc(1 + $int1);
}
return($int2);
