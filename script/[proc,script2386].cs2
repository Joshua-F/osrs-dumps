// 2386
[proc,script2386]
def_enum $enum = ~toplevel_getcomponents;
def_component $component = enum(component, component, $enum, toplevel_osm:side_container_warning);
if ($component = null) {
    return;
}
if (~script2387 = false) {
    if_sethide(true, $component);
} else {
    if_sethide(false, $component);
}
