// 7340
[proc,prepot_device_draw_contents](int $int0)
def_int $int1 = 0;
def_obj $obj = null;
while ($int1 < $int0) {
    if (cc_find(prepot_device:device_contents, $int1) = true) {
        $obj = inv_getobj(prepot_device_inv, $int1);
        ~prepot_device_draw_contents_slot($obj, inv_total(prepot_device_inv, $obj));
    }
    $int1 = calc($int1 + 1);
}
