// 6664
[proc,interact_entity_highlight_pulse_continue](npc_uid $npc_uid0, int $int1, loc $loc2, coord $coord3, obj $obj4, coord $coord5, string $string0)(boolean)
if ($npc_uid0 ! %interact_npc_uid | $int1 ! %interact_npc_creationcycle) {
    if (npc_finduid($npc_uid0) = true) {
        entityoverlay_delete_player(0);
    }
    return(false);
}
if ($loc2 ! %interact_loc_type | $coord3 ! %interact_loc_coord) {
    if (loc_find($coord3, $loc2) = true) {
        entityoverlay_delete_player(0);
    }
    return(false);
}
if ($obj4 ! %interact_obj_type | $coord5 ! %interact_obj_coord) {
    if (obj_find($coord5, $obj4) = true) {
        entityoverlay_delete_player(0);
    }
    return(false);
}
if (compare($string0, %interact_player_name) ! 0) {
    entityoverlay_delete_player(0);
    return(false);
}
return(true);
