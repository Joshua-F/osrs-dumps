// 8466
[proc,script8466]
def_objarray $objarray = ~script8468;
def_int $int0 = calc(array_size($objarray) - array_count($objarray, null, 0, -1));
def_boolean $booleanarray(5);
def_obj $obj1 = null;
def_dbrow $dbrow = null;
def_int $int3 = -1;
def_int $int4 = inv_size(inv);
def_int $int5 = 0;
def_obj $obj6 = null;
def_int $int7 = -1;
def_component $items = inventory:items;
if (~script8305(equipment_side:items, toplevel_osrs_stretch:sidemodal) = true & ~script8307(equipment_side:items) = false) {
    $items = equipment_side:items;
}
while ($int5 < $int4) {
    $obj6 = inv_getobj(inv, $int5);
    if ($obj6 ! null) {
        $int7 = array_indexof($objarray, $obj6, 0, -1);
        if ($int7 ! -1) {
            $objarray($int7) = null;
            $booleanarray($int7) = true;
            $obj1, $dbrow, $int3 = ~inv_highlight_get_tracked_item_data($int7);
            ~ui_highlight_update(1, $int7, $items, $int5, $dbrow, $int3);
        }
    }
    $int5 = calc($int5 + 1);
}
$int7 = 0;
while ($int7 < 5) {
    ~ui_highlight_set_hidden(1, $int7, ~flip_bool($booleanarray($int7)));
    $int7 = calc($int7 + 1);
}
def_component $component = ~script8488;
if ($component ! null) {
    if ($int0 > 0) {
        if (%inv_highlight_events_comid < 0) {
            cc_create($component, ^iftype_rectangle, ~script8490($component));
            %inv_highlight_events_comid = cc_getid;
            cc_setsize(1, 1, ^setsize_abs, ^setsize_abs);
            cc_settrans(255);
            cc_setoninvtransmit("script8467{inv}");
            cc_setonsubchange("script8467");
        }
    } else if (%inv_highlight_events_comid >= 0) {
        if (cc_find($component, %inv_highlight_events_comid) = true) {
            cc_delete;
        }
        %inv_highlight_events_comid = -1;
    }
}
