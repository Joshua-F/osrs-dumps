// 9474
[proc,script9474](int $int0, int $int1, int $int2)
if (~rental_world = true & inv_total(inv, league_tomato) > 0) {
    ~debug_mes("Would error as your area: <tostring($int0)> is <~script5484($int1)> (<tostring($int1)>), but we should be forcing <~script5484($int2)> (<tostring($int2)>) - We assume you are debugging.");
} else {
    ~error("Player <name>, managed to get their region selection: <tostring($int0)> is <~script5484($int1)> (<tostring($int1)>), but we should be forcing <~script5484($int2)> (<tostring($int2)>), We need to fix this.");
}
