// 6832
[proc,questjournal_overview_quest_requirement](dbrow $dbrow)(string)
def_string $string = ~get_quest_name($dbrow);
if (~quest_is_complete($dbrow) = true) {
    return("<str><$string>");
} else {
    return("<col=800000><$string>");
}
