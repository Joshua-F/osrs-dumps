// 7763
[clientscript,script7763](component $component, int $int)
if (cc_find($component, 3) = true) {
    cc_settext(tostring(%last_stand_fakelevel));
    if (%last_stand_fakelevel = $int) {
        cc_setontimer(null);
    }
}
