// 3759
[clientscript,recipe_for_disaster_animate](component $component, int $int1, int $int2, seq $seq)
if (clientclock >= $int2 & cc_find($component, $int1) = true) {
    cc_setontimer(null);
    cc_setmodelanim($seq);
}
