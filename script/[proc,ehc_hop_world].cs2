// 7573
[proc,ehc_hop_world](int $int)
if (~on_enhanced_any = false) {
    return;
}
if (~rental_world = false & ($int = 0 | $int > 5000 | $int <= 260)) {
    mes("That player is not on an Old School RuneScape world.");
} else if (cc_find(ehc_worldhop:buttons, $int) = true) {
    mes("Attempting to switch worlds...");
    cc_triggerop(1);
} else {
    mes("Unable to join that player's world.");
}
