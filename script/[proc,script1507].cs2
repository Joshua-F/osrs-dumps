// 1507
[proc,script1507](component $component0)
def_enum $enum1 = ~toplevel_getcomponents;
if ($enum1 ! null) {
    if_setsize(enum(enum, int, enum_1135, $enum1), enum(enum, int, enum_1136, $enum1), ^setsize_minus, ^setsize_minus, $component0);
}
