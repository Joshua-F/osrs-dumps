// 8926
[clientscript,script8926](component $component, int $int1, dbrow $dbrow, int $int3)
if (clientclock < $int3) {
    return;
}
if (cc_find($component, $int1) = true) {
    cc_setonop("port_task_board_onop(event_com, event_comsubid, $dbrow)");
    cc_setontimer(null);
}
