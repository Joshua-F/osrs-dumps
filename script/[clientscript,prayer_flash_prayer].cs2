// 3403
[clientscript,prayer_flash_prayer](int $int)
def_component $component = enum(int, component, enum_859, $int);
if ($component = null) {
    return;
}
if (cc_find($component, 1) = true) {
    if_setontimer("prayer_flash_timer($component, 0, 0)", $component);
}
