// 3553
[clientscript,poh_costumes_buttons_1off_reset](component $component, graphic $graphic, obj $obj, int $int)
if (clientclock >= $int) {
    ~poh_costumes_buttons_1off_reset($component, $graphic, $obj);
}
