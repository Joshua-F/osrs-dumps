// 3938
[proc,script3938]
def_enum $enum = ~toplevel_getcomponents;
def_component $component = null;
if (~toplevel_is_mobile($enum) = true) {
    $component = enum(component, component, $enum, toplevel_osm:control);
    ~toplevel_resize($component, $enum);
    ~toplevel_setup_popout($enum);
}
