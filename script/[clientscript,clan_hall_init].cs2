// 4330
[clientscript,clan_hall_init](component $frame, component $container, component $list, component $radio_buttons, component $number_fields, int $int5, component $scrollbar, component $save, component $back)
def_int $int9 = ~steelborder($frame, "Clan hall settings", 0);
cc_deleteall($container);
if (%clan_editing_permitted = 1) {
    ~stonebutton_init("<col=0dc10d>Save</col>", $save, p12_full, 0);
    if_setonop("opsound(event_op, 2)", $save);
} else {
    ~stonebutton_init("<col=808080>Save</col>", $save, p12_full, 0);
}
~stonebutton_init("<col=ff0000>Back</col>", $back, p12_full, 0);
if_setonop("opsound(event_op, 2)", $back);
def_int $int10 = 0;
def_int $int11 = 0;
def_int $int12 = 0;
def_int $int13 = 0;
def_int $int14 = 0;
def_int $int15 = 0;
while ($int11 < enum_getoutputcount(enum_3800)) {
    $int10, $int12, $int13, $int14, $int15 = ~script4331($int10, $int11, $list, $radio_buttons, $number_fields, $int5, $int12, $int13, $int14, $int15);
    $int11 = calc($int11 + 1);
}
if_setscrollpos(0, 0, $list);
if ($int12 > if_getheight($list)) {
    if_setscrollsize(0, $int12, $list);
} else {
    if_setscrollsize(0, 0, $list);
}
~scrollbar_vertical($scrollbar, $list, "scrollbar_dragger_v2,3", "scrollbar_dragger_v2,0", "scrollbar_dragger_v2,1", "scrollbar_dragger_v2,2", "scrollbar_v2,0", "scrollbar_v2,1");
