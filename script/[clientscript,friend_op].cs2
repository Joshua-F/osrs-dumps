// 126
[clientscript,friend_op](int $int, string $string)
if ($int = 1) {
    ~meslayer_mode6(removetags($string));
} else if ($int = 2) {
    mes("That player is currently offline.");
} else if ($int = 3) {
    friend_del(removetags($string));
}
