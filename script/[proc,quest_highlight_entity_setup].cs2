// 4873
[proc,quest_highlight_entity_setup]
highlight_npctype_clear(13);
highlight_loctype_clear(16);
def_dbrow $dbrow = null;
if (%varplayerbit_9619 = 1) {
    if (%varplayer_3679 ! null) {
        ~quest_highlight_entity(%varplayer_3679);
    }
} else if (%varplayerbit_9619 = 2) {
    db_listall(dbtable_0);
    $dbrow = db_findnext;
    while ($dbrow ! null) {
        ~quest_highlight_entity($dbrow);
        $dbrow = db_findnext;
    }
}
