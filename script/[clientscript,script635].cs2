// 635
[clientscript,script635](struct $struct, int $int1)
def_component $buff_listeners = buff_bar:buff_listeners;
def_int $int3 = struct_param($struct, param_1275);
~buff_bar_emerge_counter($struct, $int1);
if (cc_find($buff_listeners, $int3) = true) {
    ~buff_bar_start($struct);
    ~buff_bar_layout_redraw;
}
