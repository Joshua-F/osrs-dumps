// 8362
[proc,script8362](int $int0)(int)
cc_children_find(2);
if_children_filter(param_2365, 401);
def_int $int1 = 0;
while (.if_children_findnext = true) {
    $int1 = calc($int1 + paraheight(.cc_gettext, calc($int0 - 4 * 2), p12_full));
}
def_int $int2 = calc(max(1, $int1) * 16 + 4 * 2);
def_int $int3 = 0;
if (testbit(cc_param(param_2362), 1) = ^true) {
    $int2 = calc($int2 + 4);
    cc_children_find(1);
    if_children_filter(param_2365, 350);
    while (if_children_findnext = true) {
        $int3 = ~script8416($int0);
        cc_setposition(0, $int2, ^setpos_abs_left, ^setpos_abs_top);
        cc_setsize(0, $int3, ^setsize_minus, ^setsize_abs);
        $int2 = calc($int2 + $int3);
    }
    cc_find_parent;
}
return($int2);
