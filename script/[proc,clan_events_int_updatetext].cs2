// 4421
[proc,clan_events_int_updatetext](enum $enum)
def_string $string = "";
if ($enum = enum_3814) {
    $string = fromdate(~script4422($enum));
} else if ($enum = enum_3816) {
    $string = ~current_date_time(0, ~script4422($enum));
} else {
    $string = tostring(~script4422($enum));
}
cc_settext($string);
