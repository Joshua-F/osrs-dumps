// 6833
[proc,questjournal_overview_skill_requirement](stat $stat, int $int)(string)
def_string $string = enum(stat, string, enum_680, $stat);
if (stat_base($stat) >= $int) {
    return("<str>Level <tostring($int)> <$string>");
} else {
    return("<col=800000>Level <tostring($int)> <$string>");
}
