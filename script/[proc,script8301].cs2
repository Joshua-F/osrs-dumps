// 8301
[proc,script8301](dbrowarray $dbrowarray0)
def_int $int0 = array_size($dbrowarray0);
def_dbrow $dbrow1 = null;
while ($int0 > 0) {
    $int0 = calc($int0 - 1);
    $dbrow1 = $dbrowarray0($int0);
    if ($dbrow1 ! null & ~quest_is_complete($dbrow1) = true) {
        array_delete($dbrowarray0, $int0);
    }
}
