// 8687
[proc,script8687]
def_dbrow $dbrow = null;
if (~script8688 ! -1 & ~script8694 ! -1) {
    db_find(sailing_sea:unique_id, ~script8694);
    $dbrow = db_findnext;
    $dbrow = db_getfield($dbrow, sailing_sea:ocean, 0);
    if ($dbrow ! null & ~script8688 ! db_getfield($dbrow, sailing_sea:unique_id, 0)) {
        %sailing_charting_sea_filter = -1;
        ~script8661;
    }
}
