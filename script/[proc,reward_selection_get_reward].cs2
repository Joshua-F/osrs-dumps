// 8292
[proc,reward_selection_get_reward](dbrow $dbrow0, int $int)(dbrow)
def_dbrow $dbrow2 = null;
if ($int < 0) {
    return($dbrow2);
}
if ($dbrow0 ! null & $int < db_getfieldcount($dbrow0, reward_selection:reward)) {
    $dbrow2 = db_getfield($dbrow0, reward_selection:reward, $int);
}
return($dbrow2);
