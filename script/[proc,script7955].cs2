// 7955
[proc,script7955](dbrow $dbrow, int $int)
switch_dbrow ($dbrow) {
    case jigsaw_scrambled :
        %scrambled_puzzle = setbit(%scrambled_puzzle, $int);
    case default :
        %jigsaw_puzzle_solution = setbit(%jigsaw_puzzle_solution, $int);
}
