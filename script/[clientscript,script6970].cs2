// 6970
[clientscript,script6970](component $component, string $string, int $int, boolean $boolean)
if (clientclock < $int) {
    return;
}
if ($boolean = true) {
    ~combination_lock_initbutton_confirm($component, $string);
} else {
    ~combination_lock_initbutton_confirm($component, $string);
}
