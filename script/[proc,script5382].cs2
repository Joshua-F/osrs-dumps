// 5382
[proc,script5382](dbrow $dbrow, int $int)(int)
switch_dbrow ($dbrow) {
    case tithe_shop_autoweed_toggle :
        if (%farming_blockweeds > 0) {
            return(0);
        }
    case tithe_shop_watering_can_recharge :
        $int = %omnishop_alternate_cost;
}
return($int);
