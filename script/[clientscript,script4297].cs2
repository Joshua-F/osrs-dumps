// 4297
[clientscript,script4297](boolean $boolean, string $string, component $component, graphic $graphic)
~clan_stonebutton_draw($boolean, $string, $graphic, $component);
if ($boolean = true) {
    if_setontimer("script4298(calc(clientclock + 2), $string, event_com, $graphic)", $component);
} else {
    if_setontimer(null, $component);
}
