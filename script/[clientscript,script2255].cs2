// 2255
[clientscript,script2255](component $component, int $int1, int $int2, int $int3, int $int4, int $int5)
if (calc(clientclock - $int1) < 15) {
    return;
}
def_int $int6 = 0;
if (cc_find($component, $int3) = true) {
    $int6 = calc(cc_getwidth - $int5);
    if ($int6 <= 0) {
        if (cc_find($component, $int2) = true) {
            cc_delete;
        }
        if (cc_find($component, $int3) = true) {
            cc_delete;
        }
        if (cc_find($component, $int4) = true) {
            cc_delete;
        }
        if_setontimer(null, $component);
    } else {
        if (cc_find($component, $int2) = true) {
            cc_setsize($int6, cc_getheight, ^setsize_abs, ^setsize_abs);
        }
        if (cc_find($component, $int3) = true) {
            cc_setsize($int6, cc_getheight, ^setsize_abs, ^setsize_abs);
        }
        if (cc_find($component, $int4) = true) {
            cc_setsize($int6, cc_getheight, ^setsize_abs, ^setsize_abs);
        }
    }
} else {
    if_setontimer(null, $component);
}
