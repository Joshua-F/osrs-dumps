// 1737
[clientscript,worldmap_search_input](int $key, char $char, component $component, int $int3, int $int4, coord $coord)
if (~keypress_permit(null, interface_595) = false) {
    return;
}
if (%varclient_190 = 1 & cc_find($component, $int3) = true & .cc_find($component, $int4) = true) {
    if ($key = ^key_return) {
        if ($coord ! null) {
            sound_synth(synth_2266, 1, 0);
            worldmap_jumptodisplaycoord($coord);
            ~worldmap_search_off($component);
        }
        ~keyboard_hide;
        return;
    }
    if ($key = ^key_escape) {
        sound_synth(synth_2266, 1, 0);
        ~worldmap_search_off($component);
        ~keyboard_hide;
        return;
    }
    %varclient_363 = ~add_to_inputstring(%varclient_363, 4, $key, $char);
    ~worldmap_search_update("", $component);
}
