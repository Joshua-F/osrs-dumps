// 1509
[clientscript,script1509]
def_int $int = 0;
if (%chat_background_invisible_timeout < clientclock) {
    $int = 1;
}
%chat_background_invisible_timeout = calc(clientclock + 20);
if ($int = 1) {
    ~toplevel_chatbox_background;
}
