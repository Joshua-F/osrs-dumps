// 5471
[clientscript,add_overlaytimer_npc](npc_uid $npc_uid, int $int1, int $int2, int $rgb, int $int4)
if (~on_enhanced = false) {
    return;
}
if (npc_finduid($npc_uid) = true) {
    ~add_overlaytimer_npc($int1, $int2, $rgb, $int4);
} else {
    ~debug_mes("Overlay Timers: Unable to find NPC for overlay timer <tostring($int1)>.");
}
