// 9042
[proc,sailing_sidepanel_crew_count_get]()(int)
def_int $int0 = 0;
def_int $int1 = 0;
def_int $int2 = 0;
def_int $int3 = 5;
while ($int2 < $int3) {
    $int1 = ~sailing_sidepanel_crew_get_slot($int2);
    if ($int1 ! 0) {
        $int0 = calc($int0 + 1);
    }
    $int2 = calc($int2 + 1);
}
if (%sailing_sidepanel_boat_type ! null) {
    $int0 = min($int0, db_getfield(%sailing_sidepanel_boat_type, sailing_boat:crew_capacity, 0));
}
return($int0);
