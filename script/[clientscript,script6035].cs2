// 6035
[clientscript,script6035](component $component, graphic $graphic, string $string, int $int, boolean $boolean)
if (clientclock < $int) {
    return;
}
if ($boolean = true) {
    ~bcs_pillar_initbutton_confirm($component, $graphic, $string);
} else {
    ~bcs_pillar_initbutton_confirm($component, $graphic, $string);
}
