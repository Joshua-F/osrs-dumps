// 4453
[clientscript,script4453](boolean $boolean, string $string, component $component, graphic $graphic)
if (%varclient_454 = 1) {
    ~clan_stonebutton_draw(true, $string, $graphic, $component);
    if_setontimer(null, $component);
    return;
}
~clan_stonebutton_draw($boolean, $string, $graphic, $component);
if ($boolean = true) {
    if_setontimer("script4298(calc(clientclock + 2), $string, event_com, $graphic)", $component);
} else {
    if_setontimer(null, $component);
}
