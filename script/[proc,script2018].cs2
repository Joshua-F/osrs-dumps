// 2018
[proc,script2018](component $component)
def_enum $enum = ~toplevel_getcomponents;
if ($enum = null) {
    return;
}
def_int $int = enum(enum, int, enum_1135, $enum);
if_setsize($int, enum(enum, int, enum_1136, $enum), ^setsize_minus, ^setsize_minus, $component);
