// 8743
[proc,script8743](int $int0, component $component1, dbrow $dbrow2)
if ($dbrow2 = sailing_sidepanel_facility_button_wind_catcher_button) {
    $int0 = calc($int0 - 2);
    if (cc_find($component1, $int0) = ^true) {
        cc_setontimer("script8744($int0, $component1, 0)");
    }
} else if ($dbrow2 = sailing_sidepanel_facility_hull_text & cc_find($component1, calc($int0 - 1)) = ^true) {
    if (%sailing_sidepanel_repairkits >= 16383) {
        cc_settext("Repairs:<br>Lots!");
    } else {
        cc_settext("Repairs:<br><~tostring_spacer(%sailing_sidepanel_repairkits, ",")> HP");
    }
}
