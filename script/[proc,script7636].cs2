// 7636
[proc,script7636](int $int, component $component)
db_find(dbtable_85:col7, $int);
def_dbrow $dbrow = db_findnext;
if (cc_find($component, 1) = true) {
    ~league_area_details_draw_boss($dbrow, $component);
}
