// 7143
[proc,omnishop_main_list_setup](dbrow $dbrow, inv $inv, int $int2, int $int3)
def_component $list = omnishop_main:list;
def_component $list_scroller = omnishop_main:list_scroller;
cc_deleteall($list);
def_int $int6 = 0;
def_int $int7 = 0;
def_boolean $boolean = false;
def_int $int9 = 0;
def_int $int10 = 0;
def_int $int11 = 0;
def_int $int12 = db_getfieldcount($dbrow, omnishop_shop_data:omnishop_shop_stock);
if ($int12 = 0) {
    $int12 = inv_size($inv);
}
while ($int6 < $int12) {
    if ($int6 = $int2 & %omnishop_view_expanded = 0) {
        $int9 = 1;
    } else {
        $int9 = 0;
    }
    $boolean, $int10 = ~omnishop_main_list_item_setup($int6, $int7, $dbrow, $inv, $int9);
    if ($boolean = true) {
        $int7 = calc($int7 + 1);
        $int11 = $int10;
    }
    $int6 = calc($int6 + 1);
}
if ($int11 > if_getheight($list)) {
    if_setscrollsize(0, $int11, $list);
    ~scrollbar_vertical($list_scroller, $list, "scrollbar_dragger_v2,3", "scrollbar_dragger_v2,0", "scrollbar_dragger_v2,1", "scrollbar_dragger_v2,2", "scrollbar_v2,0", "scrollbar_v2,1");
    if (cc_find($list_scroller, 1) = true) {
        ~scrollbar_vertical_doscroll($list_scroller, $list, %omnishop_scroll_pos, true);
    }
} else {
    %omnishop_scroll_pos = 0;
    if_setscrollsize(0, 0, $list);
    ~scrollbar_vertical($list_scroller, $list, "scrollbar_dragger_v2,3", "scrollbar_dragger_v2,0", "scrollbar_dragger_v2,1", "scrollbar_dragger_v2,2", "scrollbar_v2,0", "scrollbar_v2,1");
    if (cc_find($list_scroller, 1) = true) {
        ~scrollbar_vertical_doscroll($list_scroller, $list, 0, true);
    }
}
