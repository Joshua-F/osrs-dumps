// 915
[clientscript,toplevel_sidebutton_switch](int $int)
def_enum $enum = ~toplevel_getcomponents;
if ($enum = null) {
    return;
}
if ($int = -1 & enum(enum, boolean, enum_1134, $enum) = false) {
    $int = 9;
}
~toplevel_sidebutton_switch($int, $enum);
