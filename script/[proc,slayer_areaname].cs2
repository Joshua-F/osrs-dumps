// 8057
[proc,slayer_areaname](int $int0, int $int1)(string)
def_dbrow $dbrow = ~script8054($int0);
if ($dbrow = null) {
    ~script2075("Area id <tostring($int0)> does not have a dbrow configured.");
    return("");
}
if ($int1 = 0) {
    return(db_getfield($dbrow, slayer_area:area_text, 0));
} else {
    return(db_getfield($dbrow, slayer_area:area_name_in_helper, 0));
}
