// 8969
[proc,sailing_boat_selection_get_boat_type](int $int)(string)
db_find(sailing_boat:type_id, $int);
def_dbrow $dbrow = db_findnext;
if ($dbrow = null) {
    return("Unknown");
}
def_string $string = db_getfield($dbrow, sailing_boat:name, 0);
return($string);
