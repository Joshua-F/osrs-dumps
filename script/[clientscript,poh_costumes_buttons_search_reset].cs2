// 3564
[clientscript,poh_costumes_buttons_search_reset](enum $enum, component $component, graphic $graphic)
if (%meslayermode ! 17) {
    ~poh_costumes_buttons_search_reset($enum, $component, $graphic);
}
