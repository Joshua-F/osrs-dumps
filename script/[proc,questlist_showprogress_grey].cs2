// 3239
[proc,questlist_showprogress_grey](struct $struct)(boolean)
def_enum $enum = null;
if (~league_world = true) {
    $enum = struct_param(enum(int, struct, enum_2670, %varplayerbit_10032), param_890);
    if ($enum ! null & enum(struct, boolean, $enum, $struct) = false) {
        return(true);
    }
}
if (~speedrunning_world = true & ~speedrunning_quest_enabled($struct) = false) {
    return(true);
}
return(false);
