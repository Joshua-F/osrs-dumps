// 7961
[proc,jigsaw_puzzle_draw](dbrow $dbrow0, int $int1)
if ($int1 = 0) {
    ~script7962($dbrow0);
    ~script7972($dbrow0);
    ~jigsaw_preview_button;
    ~jigsaw_draw_preview($dbrow0);
}
~script7963($dbrow0);
