// 7961
[proc,jigsaw_puzzle_draw](dbrow $dbrow, int $int)
if ($int = 0) {
    ~jigsaw_puzzle_draw_backing($dbrow);
    ~script7972($dbrow);
    ~jigsaw_preview_button;
    ~jigsaw_draw_preview($dbrow);
}
~jigsaw_puzzle_draw_pieces($dbrow);
