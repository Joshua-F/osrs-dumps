// 3582
[proc,hallowed_tools_getcarried](obj $obj0)(int, obj)
switch_obj ($obj0) {
    case hallowed_token :
        return(~farming_tools_countcarried(hallowed_token), hallowed_token);
    case hallowed_grapple :
        return(~farming_tools_countcarried(hallowed_grapple), hallowed_grapple);
    case hallowed_focus :
        return(~farming_tools_countcarried(hallowed_focus), hallowed_focus);
    case hallowed_symbol :
        return(~farming_tools_countcarried(hallowed_symbol), hallowed_symbol);
    case hallowed_hammer :
        return(~farming_tools_countcarried(hallowed_hammer), hallowed_hammer);
    case hallowed_ring :
        return(~farming_tools_countcarried(hallowed_ring), hallowed_ring);
}
return(0, null);
