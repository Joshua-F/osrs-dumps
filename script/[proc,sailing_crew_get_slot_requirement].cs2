// 9048
[proc,sailing_crew_get_slot_requirement](int $int0)(int)
def_int $int1 = ^max_32bit_int;
if ($int0 < 0 | $int0 > 4) {
    ~error("Input invalid ID.");
    return($int1);
}
switch_int ($int0) {
    case default :
        ~error("Input ID not found, may need adding to switch case.");
        return($int1);
    case 0 :
        return(40);
    case 1 :
        return(55);
    case 2 :
        return(70);
    case 3 :
        return(85);
    case 4 :
        return(95);
}
~error("Input ID not found, may need adding to switch case.");
return($int1);
