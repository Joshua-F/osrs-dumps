// 3125
[clientscript,poh_board_send_name](component $component, int $int1, string $string, int $int2)
if (clientclock < $int2) {
    return;
}
resume_namedialog($string);
if (cc_find($component, $int1) = true) {
    cc_setontimer(null);
}
