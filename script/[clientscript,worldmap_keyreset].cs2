// 1729
[clientscript,worldmap_keyreset](category $category, component $component, int $int2, int $int3)
if (clientclock < $int3) {
    return;
}
if (cc_find($component, $int2) = true) {
    ~worldmap_keybutton($category, $component, $int2);
}
