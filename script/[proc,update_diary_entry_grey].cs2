// 3235
[proc,update_diary_entry_grey](int $int)(boolean)
def_enum $enum = null;
if (~league_world = true) {
    $enum = struct_param(enum(int, struct, enum_2670, %varplayerbit_10032), param_891);
    if ($enum ! null & enum(int, boolean, $enum, $int) = false) {
        return(true);
    }
}
return(false);
