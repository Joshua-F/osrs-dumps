// 1973
[clientscript,osm_battery_reset](component $component, int $int)
if (clientclock >= $int) {
    if ($int = -1) {
        sound_synth(synth_2266, 1, 0);
    }
    ~osm_battery_init($component);
}
