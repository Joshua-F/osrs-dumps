// 9564
[proc,script9564](int $int0, obj $obj, int $int2, int $int3)(int)
if ($int2 < 0 | $int3 < 0 | $int2 >= 8 | $int3 >= 10) {
    return(-2);
}
$obj = oc_uncert($obj);
$obj = oc_unplaceholder($obj);
switch_int ($int0) {
    case 0 :
        array_push(%banktags_t0_items, $obj);
        array_push(%banktags_t0_xcoords, $int2);
        array_push(%banktags_t0_ycoords, $int3);
        return(calc(array_size(%banktags_t0_items) - 1));
    case 1 :
        array_push(%banktags_t1_items, $obj);
        array_push(%banktags_t1_xcoords, $int2);
        array_push(%banktags_t1_ycoords, $int3);
        return(calc(array_size(%banktags_t1_items) - 1));
    case 2 :
        array_push(%banktags_t2_items, $obj);
        array_push(%banktags_t2_xcoords, $int2);
        array_push(%banktags_t2_ycoords, $int3);
        return(calc(array_size(%banktags_t2_items) - 1));
    case 3 :
        array_push(%banktags_t3_items, $obj);
        array_push(%banktags_t3_xcoords, $int2);
        array_push(%banktags_t3_ycoords, $int3);
        return(calc(array_size(%banktags_t3_items) - 1));
    case 4 :
        array_push(%banktags_t4_items, $obj);
        array_push(%banktags_t4_xcoords, $int2);
        array_push(%banktags_t4_ycoords, $int3);
        return(calc(array_size(%banktags_t4_items) - 1));
    case 5 :
        array_push(%banktags_t5_items, $obj);
        array_push(%banktags_t5_xcoords, $int2);
        array_push(%banktags_t5_ycoords, $int3);
        return(calc(array_size(%banktags_t5_items) - 1));
    case 6 :
        array_push(%banktags_t6_items, $obj);
        array_push(%banktags_t6_xcoords, $int2);
        array_push(%banktags_t6_ycoords, $int3);
        return(calc(array_size(%banktags_t6_items) - 1));
    case 7 :
        array_push(%banktags_t7_items, $obj);
        array_push(%banktags_t7_xcoords, $int2);
        array_push(%banktags_t7_ycoords, $int3);
        return(calc(array_size(%banktags_t7_items) - 1));
    case 8 :
        array_push(%banktags_t8_items, $obj);
        array_push(%banktags_t8_xcoords, $int2);
        array_push(%banktags_t8_ycoords, $int3);
        return(calc(array_size(%banktags_t8_items) - 1));
    case 9 :
        array_push(%banktags_t9_items, $obj);
        array_push(%banktags_t9_xcoords, $int2);
        array_push(%banktags_t9_ycoords, $int3);
        return(calc(array_size(%banktags_t9_items) - 1));
    case 10 :
        array_push(%banktags_t10_items, $obj);
        array_push(%banktags_t10_xcoords, $int2);
        array_push(%banktags_t10_ycoords, $int3);
        return(calc(array_size(%banktags_t10_items) - 1));
    case 11 :
        array_push(%banktags_t11_items, $obj);
        array_push(%banktags_t11_xcoords, $int2);
        array_push(%banktags_t11_ycoords, $int3);
        return(calc(array_size(%banktags_t11_items) - 1));
    case 12 :
        array_push(%banktags_t12_items, $obj);
        array_push(%banktags_t12_xcoords, $int2);
        array_push(%banktags_t12_ycoords, $int3);
        return(calc(array_size(%banktags_t12_items) - 1));
    case 13 :
        array_push(%banktags_t13_items, $obj);
        array_push(%banktags_t13_xcoords, $int2);
        array_push(%banktags_t13_ycoords, $int3);
        return(calc(array_size(%banktags_t13_items) - 1));
    case 14 :
        array_push(%banktags_t14_items, $obj);
        array_push(%banktags_t14_xcoords, $int2);
        array_push(%banktags_t14_ycoords, $int3);
        return(calc(array_size(%banktags_t14_items) - 1));
    case 15 :
        array_push(%banktags_t15_items, $obj);
        array_push(%banktags_t15_xcoords, $int2);
        array_push(%banktags_t15_ycoords, $int3);
        return(calc(array_size(%banktags_t15_items) - 1));
    case 16 :
        array_push(%banktags_t16_items, $obj);
        array_push(%banktags_t16_xcoords, $int2);
        array_push(%banktags_t16_ycoords, $int3);
        return(calc(array_size(%banktags_t16_items) - 1));
    case 17 :
        array_push(%banktags_t17_items, $obj);
        array_push(%banktags_t17_xcoords, $int2);
        array_push(%banktags_t17_ycoords, $int3);
        return(calc(array_size(%banktags_t17_items) - 1));
    case 18 :
        array_push(%banktags_t18_items, $obj);
        array_push(%banktags_t18_xcoords, $int2);
        array_push(%banktags_t18_ycoords, $int3);
        return(calc(array_size(%banktags_t18_items) - 1));
    case 19 :
        array_push(%banktags_t19_items, $obj);
        array_push(%banktags_t19_xcoords, $int2);
        array_push(%banktags_t19_ycoords, $int3);
        return(calc(array_size(%banktags_t19_items) - 1));
}
return(-2);
