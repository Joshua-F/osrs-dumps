// 7697
[clientscript,script7697](int $int0, int $int1)
$int1 = scale(1, 10, $int1);
switch_int ($int0) {
    case 0 :
        if (%extra_xp_attack < calc(^max_32bit_int - $int1)) {
            %extra_xp_attack = calc(%extra_xp_attack + $int1);
        }
    case 2 :
        if (%extra_xp_strength < calc(^max_32bit_int - $int1)) {
            %extra_xp_strength = calc(%extra_xp_strength + $int1);
        }
    case 1 :
        if (%extra_xp_defence < calc(^max_32bit_int - $int1)) {
            %extra_xp_defence = calc(%extra_xp_defence + $int1);
        }
    case 5 :
        if (%extra_xp_prayer < calc(^max_32bit_int - $int1)) {
            %extra_xp_prayer = calc(%extra_xp_prayer + $int1);
        }
    case 4 :
        if (%extra_xp_ranged < calc(^max_32bit_int - $int1)) {
            %extra_xp_ranged = calc(%extra_xp_ranged + $int1);
        }
    case 6 :
        if (%extra_xp_magic < calc(^max_32bit_int - $int1)) {
            %extra_xp_magic = calc(%extra_xp_magic + $int1);
        }
    case 20 :
        if (%extra_xp_runecrafting < calc(^max_32bit_int - $int1)) {
            %extra_xp_runecrafting = calc(%extra_xp_runecrafting + $int1);
        }
    case 22 :
        if (%extra_xp_construction < calc(^max_32bit_int - $int1)) {
            %extra_xp_construction = calc(%extra_xp_construction + $int1);
        }
    case 3 :
        if (%extra_xp_hitpoints < calc(^max_32bit_int - $int1)) {
            %extra_xp_hitpoints = calc(%extra_xp_hitpoints + $int1);
        }
    case 16 :
        if (%extra_xp_agility < calc(^max_32bit_int - $int1)) {
            %extra_xp_agility = calc(%extra_xp_agility + $int1);
        }
    case 15 :
        if (%extra_xp_herblore < calc(^max_32bit_int - $int1)) {
            %extra_xp_herblore = calc(%extra_xp_herblore + $int1);
        }
    case 17 :
        if (%extra_xp_thieving < calc(^max_32bit_int - $int1)) {
            %extra_xp_thieving = calc(%extra_xp_thieving + $int1);
        }
    case 12 :
        if (%extra_xp_crafting < calc(^max_32bit_int - $int1)) {
            %extra_xp_crafting = calc(%extra_xp_crafting + $int1);
        }
    case 9 :
        if (%extra_xp_fletching < calc(^max_32bit_int - $int1)) {
            %extra_xp_fletching = calc(%extra_xp_fletching + $int1);
        }
    case 18 :
        if (%extra_xp_slayer < calc(^max_32bit_int - $int1)) {
            %extra_xp_slayer = calc(%extra_xp_slayer + $int1);
        }
    case 21 :
        if (%extra_xp_hunter < calc(^max_32bit_int - $int1)) {
            %extra_xp_hunter = calc(%extra_xp_hunter + $int1);
        }
    case 14 :
        if (%extra_xp_mining < calc(^max_32bit_int - $int1)) {
            %extra_xp_mining = calc(%extra_xp_mining + $int1);
        }
    case 13 :
        if (%extra_xp_smithing < calc(^max_32bit_int - $int1)) {
            %extra_xp_smithing = calc(%extra_xp_smithing + $int1);
        }
    case 10 :
        if (%extra_xp_fishing < calc(^max_32bit_int - $int1)) {
            %extra_xp_fishing = calc(%extra_xp_fishing + $int1);
        }
    case 7 :
        if (%extra_xp_cooking < calc(^max_32bit_int - $int1)) {
            %extra_xp_cooking = calc(%extra_xp_cooking + $int1);
        }
    case 11 :
        if (%extra_xp_firemaking < calc(^max_32bit_int - $int1)) {
            %extra_xp_firemaking = calc(%extra_xp_firemaking + $int1);
        }
    case 8 :
        if (%extra_xp_woodcutting < calc(^max_32bit_int - $int1)) {
            %extra_xp_woodcutting = calc(%extra_xp_woodcutting + $int1);
        }
    case 19 :
        if (%extra_xp_farming < calc(^max_32bit_int - $int1)) {
            %extra_xp_farming = calc(%extra_xp_farming + $int1);
        }
}
if (%extra_xp_total < calc(^max_32bit_int - $int1)) {
    %extra_xp_total = calc(%extra_xp_total + $int1);
}
