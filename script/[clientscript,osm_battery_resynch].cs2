// 958
[clientscript,osm_battery_resynch](component $component, int $int1, int $int2)
if (calc(clientclock % 250) = 0 & cc_find($component, $int1) = true & .cc_find($component, $int2) = true) {
    ~osm_battery_resynch;
}
