// 241
[proc,autocast_tooltip_runecount](obj $obj, int $int1)
cc_setobject($obj, 1);
def_int $int2 = ~magic_runecount($obj);
.cc_settext("<~magic_tostring($int2)>/<tostring($int1)>");
if ($int2 >= $int1) {
    .cc_setcolour(^green);
} else {
    .cc_setcolour(^red);
}
