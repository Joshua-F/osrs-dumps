// 417
[clientscript,chat_set_filter_conditional](int $int, int $chatfilter1, int $chatfilter2)
if (~chat_get_filter($int) = $chatfilter1) {
    ~chat_set_filter($int, $chatfilter2);
    ~chat_onsetupchange;
}
