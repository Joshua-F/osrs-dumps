// 4482
[clientscript,chat_button_hover](boolean $boolean, int $int)
if ($boolean = true) {
    %chat_highlight = $int;
    ~redraw_chat_buttons;
} else if (%chat_highlight = $int) {
    %chat_highlight = -1;
    ~redraw_chat_buttons;
}
