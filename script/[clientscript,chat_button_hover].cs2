// 4482
[clientscript,chat_button_hover](boolean $boolean0, int $int1)
if ($boolean0 = true) {
    %chat_highlight = $int1;
    ~redraw_chat_buttons;
} else if (%chat_highlight = $int1) {
    %chat_highlight = -1;
    ~redraw_chat_buttons;
}
