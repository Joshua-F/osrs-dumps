// 8285
[proc,script8285](struct $struct0, enum $enum1)(int, int)
if (~script8216(0) = false) {
    return(-1, -1);
}
if ($enum1 = null) {
    return(-1, -1);
}
def_int $int2 = enum(struct, int, $enum1, $struct0);
if ($int2 = -1) {
    return(-1, -1);
}
def_int $int3 = calc($int2 / 32);
def_int $int4 = calc($int2 % 32);
return($int3, $int4);
