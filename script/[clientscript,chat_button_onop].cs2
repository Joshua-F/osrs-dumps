// 175
[clientscript,chat_button_onop](int $int0, int $int1)
def_int $int2 = ~script553;
switch_int ($int1) {
    case 0 :
        switch_int ($int0) {
            case 1 :
                ~chat_button_switch($int1, $int2);
            case 2 :
                %chatbox_mode = 0;
                ~redraw_chat_buttons;
                ~script223;
            case 3 :
                %chatbox_mode = 1;
                ~redraw_chat_buttons;
                ~script223;
            case 4 :
                %chatbox_mode = 2;
                ~redraw_chat_buttons;
                ~script223;
            case 5 :
                %chatbox_mode = 3;
                ~redraw_chat_buttons;
                ~script223;
            case 6 :
                if (~is_group_iron = true) {
                    %chatbox_mode = 4;
                }
                ~redraw_chat_buttons;
                ~script223;
        }
    case 1 :
        switch_int ($int0) {
            case 1 :
                ~chat_button_switch($int1, $int2);
            case 2 :
                if (%game_filter = 0) {
                    %game_filter = 1;
                } else {
                    %game_filter = 0;
                }
                ~redraw_chat_buttons;
                ~rebuildchatbox($int2);
                ~rebuildpmbox($int2);
        }
    case 2 :
        switch_int ($int0) {
            case 1 :
                ~chat_button_switch($int1, $int2);
            case 2 :
                ~chat_set_filter($int1, ^chatfilter_autochat);
                ~redraw_chat_buttons;
                ~rebuildchatbox($int2);
                ~rebuildpmbox($int2);
            case 3 :
                ~chat_set_filter($int1, ^chatfilter_on);
                ~redraw_chat_buttons;
                ~rebuildchatbox($int2);
                ~rebuildpmbox($int2);
            case 4 :
                ~chat_set_filter($int1, ^chatfilter_friends);
                ~redraw_chat_buttons;
                ~rebuildchatbox($int2);
                ~rebuildpmbox($int2);
            case 5 :
                ~chat_set_filter($int1, ^chatfilter_off);
                ~redraw_chat_buttons;
                ~rebuildchatbox($int2);
                ~rebuildpmbox($int2);
            case 6 :
                ~chat_set_filter($int1, ^chatfilter_hide);
                ~redraw_chat_buttons;
                ~rebuildchatbox($int2);
                ~rebuildpmbox($int2);
            case 7 :
                %public_chat_cleared_time = clientclock;
                ~rebuildchatbox($int2);
            case 8 :
                ~meslayer_mode12;
            case 9 :
                ~meslayer_mode13;
            case 10 :
                if (string_length(%autotyper) > 0) {
                    if (%autochat_pause = 1) {
                        %autochat_pause = 0;
                    } else {
                        %autochat_pause = 1;
                    }
                    ~chat_autotyper_updateop;
                } else {
                    %autochat_pause = 0;
                    ~chat_autotyper_updateop;
                }
        }
    case 3 :
        switch_int ($int0) {
            case 1 :
                ~chat_button_switch($int1, $int2);
            case 3 :
                ~chat_set_filter($int1, ^chatfilter_on);
                ~redraw_chat_buttons;
                ~rebuildchatbox($int2);
                ~rebuildpmbox($int2);
            case 4 :
                ~chat_set_filter($int1, ^chatfilter_friends);
                ~redraw_chat_buttons;
                ~rebuildchatbox($int2);
                ~rebuildpmbox($int2);
            case 5 :
                ~chat_set_filter($int1, ^chatfilter_off);
                ~redraw_chat_buttons;
                ~rebuildchatbox($int2);
                ~rebuildpmbox($int2);
            case 7 :
                %private_chat_cleared_time = clientclock;
                if (%option_pm = 0) {
                    ~rebuildchatbox($int2);
                } else {
                    ~rebuildpmbox($int2);
                }
        }
    case 4 :
        switch_int ($int0) {
            case 1 :
                ~chat_button_switch($int1, $int2);
            case 3 :
                ~chat_set_filter($int1, ^chatfilter_on);
                ~redraw_chat_buttons;
                ~rebuildchatbox($int2);
                ~rebuildpmbox($int2);
            case 4 :
                ~chat_set_filter($int1, ^chatfilter_friends);
                ~redraw_chat_buttons;
                ~rebuildchatbox($int2);
                ~rebuildpmbox($int2);
            case 5 :
                ~chat_set_filter($int1, ^chatfilter_off);
                ~redraw_chat_buttons;
                ~rebuildchatbox($int2);
                ~rebuildpmbox($int2);
            case 7 :
                %friendschat_cleared_time = clientclock;
                ~rebuildchatbox($int2);
        }
    case 5 :
        switch_int ($int0) {
            case 1 :
                ~chat_button_switch($int1, $int2);
            case 3 :
                ~chat_set_filter($int1, ^chatfilter_on);
                ~redraw_chat_buttons;
                ~rebuildchatbox($int2);
                ~rebuildpmbox($int2);
            case 4 :
                ~chat_set_filter($int1, ^chatfilter_friends);
                ~redraw_chat_buttons;
                ~rebuildchatbox($int2);
                ~rebuildpmbox($int2);
            case 5 :
                ~chat_set_filter($int1, ^chatfilter_off);
                ~redraw_chat_buttons;
                ~rebuildchatbox($int2);
                ~rebuildpmbox($int2);
            case 7 :
                %clan_chat_cleared_time = clientclock;
                ~rebuildchatbox($int2);
        }
    case 6 :
        switch_int ($int0) {
            case 1 :
                ~chat_button_switch($int1, $int2);
            case 3 :
                ~chat_set_filter($int1, ^chatfilter_on);
                ~redraw_chat_buttons;
                ~rebuildchatbox($int2);
                ~rebuildpmbox($int2);
            case 4 :
                ~chat_set_filter($int1, ^chatfilter_friends);
                ~redraw_chat_buttons;
                ~rebuildchatbox($int2);
                ~rebuildpmbox($int2);
            case 5 :
                ~chat_set_filter($int1, ^chatfilter_off);
                ~redraw_chat_buttons;
                ~rebuildchatbox($int2);
                ~rebuildpmbox($int2);
            case 7 :
                if (~is_group_iron = true) {
                    %group_chat_cleared_time = clientclock;
                    ~rebuildchatbox($int2);
                }
        }
}
