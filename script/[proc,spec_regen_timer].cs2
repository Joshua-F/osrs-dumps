// 4723
[proc,spec_regen_timer](int $int0, int $int1)
if (~on_enhanced_any = false) {
    return;
}
def_component $spec_regen = orbs:spec_regen;
if (~on_mobile = false & ~toplevel_getcomponents ! enum_1129 & %minimap_toggle = 1) {
    $spec_regen = orbs_nomap:spec_regen;
} else if (~on_mobile = true & %osm_minimap_toggle = 0) {
    $spec_regen = orbs_osm:spec_regen;
} else if (~on_mobile = true & %osm_minimap_toggle = 1) {
    $spec_regen = orbs_osm_nomap:spec_regen;
}
cc_deleteall($spec_regen);
if_setonvartransmit("spec_regen_vartransmit($spec_regen, %regen_indicator_disabled, %sa_energy){settings_varp_1, sa_energy}", $spec_regen);
if (%orbs_disabled = 1) {
    return;
}
if (%regen_indicator_disabled = 1) {
    return;
}
if (%sa_energy >= 1000) {
    return;
}
$int0 = calc($int0 * 30);
if ($int0 = 0) {
    $int1 = 0;
} else if ($int1 >= $int0) {
    $int1 = calc($int1 % $int0);
}
cc_create($spec_regen, ^iftype_circle, 0);
cc_setsize(0, 0, ^setsize_minus, ^setsize_minus);
cc_setposition(0, 0, ^setpos_abs_centre, ^setpos_abs_centre);
cc_setcolour(0x03cffd);
cc_setfill(false);
cc_setlinewid(2);
cc_setarc(0, 0);
if_setontimer("spec_regen_arc($spec_regen, 0, $int1, $int0)", $spec_regen);
