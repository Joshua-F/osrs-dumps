// 708
[proc,script708]()(boolean)
def_int $int = 0;
def_inv $inv = enum(int, inv, enum_150, $int);
while ($inv ! null) {
    if (inv_getnum($inv, 0) > 0 | inv_getnum($inv, 1) > 0) {
        return(true);
    }
    $int = calc($int + 1);
    $inv = enum(int, inv, enum_150, $int);
}
return(false);
