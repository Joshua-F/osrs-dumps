// 4866
[proc,script4866](dbrow $dbrow)(dbrow)
if (db_getrowtable($dbrow) = speedrun) {
    if (db_getfieldcount($dbrow, speedrun:quest) > 0) {
        return(db_getfield($dbrow, speedrun:quest, 0));
    }
    return(null);
}
return(null);
