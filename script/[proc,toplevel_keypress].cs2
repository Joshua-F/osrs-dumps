// 2068
[proc,toplevel_keypress](int $key, enum $enum, boolean $boolean2)
if (~keypress_permit(null, null) = false) {
    return;
}
def_boolean $boolean3 = false;
def_component $component4 = enum(component, component, $enum, toplevel_osrs_stretch:mainmodal);
def_component $component5 = enum(int, component, enum_1137, 3);
def_component $component6 = enum(component, component, $enum, toplevel_osrs_stretch:floater);
$component5 = enum(component, component, $enum, $component5);
if ($key = ^key_escape) {
    switch_int (%meslayermode) {
        case 14, 17, 18, 11 :
            return;
    }
    if (%keybinding_esc_to_close = 1) {
        $boolean3 = ~script1205;
        if ($component6 ! null & if_hassub($component6) = true) {
            return;
        }
        if ($component4 ! null & if_hassub($component5) = true & if_hassub($component4) = true) {
            if_close;
            return;
        }
        $component4 = enum(component, component, $enum, toplevel_osrs_stretch:sidemodal);
        if ($component4 ! null & if_hassub($component5) = true & if_hassub($component4) = true) {
            if_close;
            return;
        }
        if (if_hassub(chatbox:chatmodal) = true) {
            if_close;
            return;
        }
        if ($boolean3 = true) {
            return;
        }
    }
}
def_int $int = ~keybind_get_slot($key);
if ($int = -1 | $int = calc(%flashside - 1)) {
    return;
}
def_component $component8 = enum(int, component, enum_1137, $int);
$component8 = enum(component, component, $enum, $component8);
if (if_hassub($component8) = true) {
    ~toplevel_sidebutton_op($enum, $int, $boolean2, 0);
}
