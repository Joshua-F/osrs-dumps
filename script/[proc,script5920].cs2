// 5920
[proc,script5920](dbrow $dbrow)(int)
def_int $int1 = db_getfield($dbrow, furniture:upgrade_source_relative, 0);
if ($int1 ! 0) {
    return(1);
}
def_int $int2 = db_getfieldcount($dbrow, furniture:upgrade_source_absolute);
if ($int2 ! 0) {
    return(1);
}
return(0);
