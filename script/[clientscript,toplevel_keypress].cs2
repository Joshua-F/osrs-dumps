// 905
[clientscript,toplevel_keypress](int $key0, enum $enum1)
def_int $int2 = enum(int, int, enum_1140, $key0);
if ($int2 = -1 | $int2 = calc(%varplayerbit_3756 - 1)) {
    return;
}
def_component $component3 = enum(int, component, enum_1137, $int2);
$component3 = enum(component, component, $enum1, $component3);
if (if_hassub($component3) = true) {
    ~toplevel_sidebutton_switch($int2, $enum1);
}
