// 905
[clientscript,toplevel_keypress](int $key, enum $enum)
def_int $int = enum(int, int, enum_1140, $key);
if ($int = -1 | $int = calc(%varplayerbit_3756 - 1)) {
    return;
}
def_component $component = enum(int, component, enum_1137, $int);
$component = enum(component, component, $enum, $component);
if (if_hassub($component) = true) {
    ~toplevel_sidebutton_switch($int, $enum);
}
