// 6059
[proc,script6059](int $int)
if (%last_spec_regen_synch <= 0) {
    return;
}
$int = calc($int - %last_spec_regen_synch);
$int = calc($int * 30);
~spec_regen_timer(getbit_range(%last_regen_rate, 11, 21), $int);
