// 5222
[proc,entity_highlight_active_loc_linked_tree](coord $coord, int $int)(coord)
if ($int = 0) {
    return($coord);
} else if (calc(($int + 1) % 2) = 1) {
    $coord = movecoord($coord, 2, -1, 2);
} else {
    $coord = movecoord($coord, -2, 1, -2);
}
return($coord);
